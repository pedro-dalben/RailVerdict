---
name: Bug report
about: RailVerdict correctness, CLI, packaging, compatibility, or fail-closed defect
labels: bug
---

**Environment**
- RailVerdict: `railverdict --version`
- Ruby: `ruby -v`
- Rails: (if applicable)
- OS: 
- Command: `railverdict ...` + args, `--format json` snippet if relevant
- Config (secrets removed): paste `.railverdict.yml`

**Expected**
What GateResult / exit code / JSON / receipt state did you expect?

**Actual**
What happened? Paste `operational_failures`, `doctor` output, exit code.

**Reproduction**
Minimal synthetic fixture or steps (avoid private source).

