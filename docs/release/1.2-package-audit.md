# RailVerdict 1.2.0 — Package Audit

**Date:** 2026-08-24
**Commit:** d49c204 (v1.2.0)
**Artifacts:** `/tmp/rail_verdict-1.2.0.gem` (112640 bytes), `/tmp/rail_verdict-1.2.0-rubygems.gem` (RubyGems download) — SHA256 `d8e848b1e72585b7f583a85d2a67e25b472bd1860549d1d4330c8a33104f82e9`
**Gemspec:** `rail_verdict.gemspec` — `required_ruby_version >=3.3`, `mfa_required true`

## Build provenance

- Workflow `.github/workflows/release.yml` — build-once: `gem build` in `build` job, `sha256sum`, `gem spec files` check for `exe/railverdict-minitest-reporter.rb` + `lib/rail_verdict/mcp/server.rb`, `gem install ./rail_verdict-*.gem` smoke, `upload-artifact rail-verdict-gem`, `publish` job downloads verbatim via `actions/download-artifact@v4`, OIDC `rubygems/configure-rubygems-credentials@v2.1.0`, `gem push` of exact artifact (no rebuild). Validated via `docs/release/1.2-closeout.md` §2 artifact equivalence (diff -r empty when unpacked).

## Packaged contents

Verified via `gem unpack` and `gem spec files`:

```
LICENSE
NOTICE
README.md
exe/railverdict
exe/railverdict-minitest-reporter.rb
lib/rail_verdict.rb
lib/rail_verdict/**/*.rb  (analyzers, baseline, check, cli, contracts, coverage, git, intelligence, mcp, reporters, etc.)
schemas/*.json  (18 schemas: finding, configuration v1..v1.5, result, baseline, waiver(s), ai-analysis, repair-packet, pr-intelligence, verification-receipt, receipt-validation, coverage, minitest-reporter)
```

`spec.files` is discovered via `Dir[lib/**/*.rb] + Dir[exe/*] + Dir[schemas/*.json] + %w[README LICENSE NOTICE]` — no dev fixtures, no tmp/coverage/.env, no Lab data.

## Correct inclusion checks

| Check | Result |
|---|:---:|
| `gem contents` contains no `test/`, `spec/`, `.git/`, `tmp/`, `coverage/`, `.env`, `*.log` | PASS |
| `schemas` included (required at runtime for validation) | PASS |
| `exe/railverdict-minitest-reporter.rb` included | PASS |
| No absolute repo paths | PASS |
| No undeclared runtime deps (only `json_schemer >=2.5 <3`, `mcp ~>1.2.0`) | PASS |
| License MIT + NOTICE + TRADEMARKS metadata present | PASS |
| Gem description/summary non-empty, homepage/source/changelog URIs correct | PASS |
| Size 112640 bytes reasonable | PASS |

## Leakage scan

`grep -r IntegrarPlus|PRIVATE|api_key|password -- unpacked gem` — only hits are in `README.md` firewall paragraph, `lib/rail_verdict/intelligence/secret_detector.rb` (redaction lists) and schema examples — no private source, schema, or credential leakage. No `IntegrarPlus` private source present.

## RubyGems metadata

- Name `rail_verdict`, version `1.2.0`, published `2026-08-24T16:28:04.526Z` via OIDC (no GEM_HOST_API_KEY)
- `gem_uri https://rubygems.org/gems/rail_verdict-1.2.0.gem`
- Local SHA `d8e848...` matches downloaded artifact; unpacked trees byte-identical to local build.
