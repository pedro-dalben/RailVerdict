# RailVerdict 1.4 — Agent Handoff & Evidence Reuse — Implementation Report (DRAFT)

**Status: DRAFT — 2026-08-25 — candidate 1.4.0 (abffc9b, gem 8f97a924)**
**Lab: lab/1.4-handoff (candidate, catalog 4.0, 118 scenarios baseline)**

This draft satisfies Stage 19 structure; full Lab 1.4 expansion, adversarial audit, and performance remain to complete `READY_FOR_1_4_RELEASE`.

## 1. Executive verdict

`NOT_READY_FOR_1_4_RELEASE` — core Handoff v1 + canonical Reuse implemented, CLI/MCP parity proven, `546 runs` green, `1.3` canonical closeout done, but Lab 1.4 handoff scenarios, CI-job-to-CI-job demo, and independent audit remain.

## 2. 1.3 post-release closeout

- `v1.3.0` `980e940`, gem `1405a77` verified (`gem fetch` == workflow), `CHANGELOG` `2026-08-25`, `1.3-implementation-report.md` reconciled, `Lab main@29b9636` synchronized, `118 PASS` — `RAILVERDICT_1_3_CLOSEOUT_COMPLETE`.

## 3. Stage 0 investigation result

`docs/release/1.4-stage-0-investigation.md` 15 sections: architecture map, trust layers, `FRESH != REUSABLE != GATE`, evidence taxonomy, per-analyzer predicates (RuboCop reusable, RSpec/Minitest not, SimpleCov coupled, bundler-audit DB-dependent), `PER_ANALYZER_REUSE_DEFERRED` (whole-set only for 1.4).

## 4. Freshness vs Evidence Reuse model

- `RECEIPT_FRESH`: current `VerificationIdentity` == receipt-bound.
- `EVIDENCE_REUSABLE`: previous normalized evidence sufficient under current repo/env/analyzer/dependency/config/policy/baseline/waivers/scope/provenance/completeness.
- `VERIFICATION_REQUIRED`: otherwise, run producers.
- `ANALYZER_EVIDENCE_REUSABLE`: per-analyzer (deferred).

## 5. Evidence taxonomy

Normalized `AnalyzerResult` + `Finding` (deterministic, bounded) vs raw stdout; `Finding` message via `Shared.normalize_finding_message`.

## 6. Analyzer-specific reuse decision

- **RuboCop**: `REUSABLE UNDER` source+config+rubocop versions+Ruby, fail-closed on external `inherit_from`.
- **RSpec**: `NOT REUSABLE BECAUSE` DB/schema/ENV/nondeterminism unobservable.
- **Minitest**: `NOT REUSABLE BECAUSE` same.
- **SimpleCov**: `NOT REUSABLE BECAUSE` coupled to test execution.
- **bundler-audit**: `NOT REUSABLE BECAUSE` advisory DB can change while receipt fresh; requires DB revision.

Decision: `PER_ANALYZER_REUSE_DEFERRED`.

## 7. Handoff contract

`schemas/verification-handoff-v1.schema.json` `handoff-v1`, `handoff_id = sha256(canonical)`, `receipt` + `evidence_set` + `provenance` + `source_scope`, `256 KiB`, portable, tamper `handoff_integrity_failed`, `additionalProperties: false`.

## 8. Evidence Reuse evaluator

`lib/rail_verdict/reuse.rb` `Reuse.evaluate(handoff, current_identity, current_contract)` single authority, `REUSABLE|VERIFICATION_REQUIRED|INVALID|UNAVAILABLE`, reasons `analyzer_not_reusable:rspec`, `repository_changed`, `reuse_toctou`, etc.

## 9. Current policy reevaluation

Reused evidence → current `baseline` → `waivers` → `policy` → **NEW** `GateResult` via `Comparison` + `Verification::Policy` (same canonical pipeline, `lib/rail_verdict/cli.rb:199`).

## 10. CLI

`railverdict handoff create --output PATH` (guarded `Check` → `Receipt` → `Handoff`), `inspect`, `verify` (re-observes, `TOCTOU`), `check --handoff PATH` (whole-set `REUSABLE` skips `ProcessRunner`, `VERIFICATION_REQUIRED` fallback). Machine JSON `handoff_valid`, `receipt_fresh`, `decision`.

## 11. MCP

`create_handoff`, `inspect_handoff`, `verify_handoff` in `lib/rail_verdict/mcp/tools/` + `server.rb`, `Cache#store_handoff`, delegate to same `Handoff`/`Reuse`/`Check.effective_input_paths`, bounded, no path escape, parity proven (rubocop-only `REUSABLE`, full `VERIFICATION_REQUIRED`).

## 12. Repair compatibility

`Repair::Verifier` unchanged; `RepairPacket` reference (`packet_id`) not authority; `RVLAB-16` `boundary_changed` preserved.

## 13. PR Intelligence compatibility

Digest reference only; non-authoritative.

## 14. TOCTOU

`before.digest != after.digest` → `VERIFICATION_REQUIRED reuse_toctou` (CLI `check --handoff` + MCP `verify_handoff`).

## 15. Replay / portability

`worktree_digest` change → `repository_changed`, clone `git clone` same `HEAD`/`index`/`worktree` → `REUSABLE` (rubocop-only), `test/test_handoff.rb` covers.

## 16. CI-job handoff

Job A `handoff create` → artifact → Job B `check --handoff` (GitHub Artifacts example transport, no service). Demo to be recorded.

## 17. Tests

`546 runs, 2221 assertions, 0 failures, 5 skips` + `test/test_handoff.rb` 7 runs (determinism, tamper, oversized, predicates, drift, portability). `test_mcp_receipt` 8 runs.

## 18. Lab

`lab/1.4-handoff` candidate `1.4.0` `abffc9b` `8f97a924`, catalog `4.0` (118 baseline preserved, handoff scenarios `HANDOFF-*`/`REUSE-*` pending expansion to ~150). `1.3` baseline `118 PASS` intact.

## 19. Dogfooding

Rubocop-only `REUSABLE` (no `rspec` execution) and full `VERIFICATION_REQUIRED` proven on `railverdict-lab` fixture.

## 20. Independent audit

Pending — must prove no `false REUSABLE` (P0), no policy/TOCTOU bypass (P1).

## 21. Known limitations

- SimpleCov version without coverage file skipped (not distinct).
- `1.2` receipts without `ruby_engine` remain valid but `stale` under stronger check.
- Handoff `256 KiB`, no crypto signatures (1.x trust model).

## 22. Deferred work

Per-analyzer partial reuse (`1.5`), crypto/Sigstore/PKI, distributed cache, SaaS — per ADR 0019.

## 23. Release readiness

`NOT_READY_FOR_1_4_RELEASE` — remaining: Lab 1.4 handoff scenarios + full campaign (`118→~150` `PASS`), CI-job demo, audit (`P0:0, P1:0`), perf, `VERSION=1.4.0` candidate validated, tag/publish only after explicit authorization.
