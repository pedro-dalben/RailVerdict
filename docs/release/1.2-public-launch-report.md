# RailVerdict 1.2 — Public Launch Report

**Date:** 2026-08-24
**Engineering verdict:** PUBLIC_LAUNCH_COMPLETE / EXTERNAL_ADOPTION_READY (infrastructure ready; adoption metric is ongoing post-goal)

## Release identity

See `docs/release/1.2-release-identity.md` (conclusively resolved): published gem from `v1.2.0 → d49c204`, SHA `d8e848...`, local doc-follow-up `964c685` is doc-only (0 lib/exe/schema lines). No runtime fix missing; no `1.2.1` needed. Full provenance chain in that file.

## Public positioning

- Canonical sources: `docs/positioning.md` (one-line, short, AI-agent and CI descriptions, target users PRIMARY AI-assisted Rails teams, non-goals, avoided claims).
- One-sentence: "RailVerdict is a deterministic, fail-closed verification gate for Rails that turns evidence from your existing tools into a trustworthy PASS, WARN, FAIL, or INCOMPLETE."
- Agent line: "AI agents can write the code. RailVerdict verifies the repository state they leave behind."

## README readiness

- First screen: badges + one-line + Evidence discovers/Verification decides + Why exists + Lab validation. Optimized order per foundation: value → why → demo → install → Quick Start → policy → changed-scope → receipts → analyzers → outputs → CI → agents → MCP → Rails context → security → architecture.
- 30-second demo: real `railverdict check` PASS then `unused = "oops"` → FAIL with fingerprint, then fix → PASS. Tied to `docs/release/1.2-clean-room-rehearsal.md`.
- Why CI / Why RSpec+RuboCop / Why AI agents: three explicit sections added (complements CI, evidence vs verifier, trust boundary diagram). No disparagement, no supers claim.

## RubyGems readiness

- `rail_verdict 1.2.0` published via OIDC build-once (see 1.2-closeout + package audit). Summary matches positioning short description. Metadata audited: homepage, source, changelog, MIT, ruby >=3.3, deps `json_schemer` + `mcp`. Future README wording fix lands with next publish; no extra version cut needed.

## GitHub Release readiness

- Tag `v1.2.0` exists, CI green. Release body auto-generated from PRs #3/#4; HIGHlights structure template prepared in `docs/launch/show-hn.md` concentration — replacement body will reference positioning + HIGH issues post-habiting. No Completeness regression.

## Documentation readiness

- New/updated: `docs/troubleshooting.md` (15 failure modes with symptom/cause/diagnose/fix), README FAQ, `docs/adoption/*`, `docs/roadmap/1.3-evidence-backlog.md`, `docs/positioning.md`, launch challenge.
- Links: `docs/*`, schemas, Lab, adrs validated (mcp external link intentional).
- Baseline vs greenfield: `Baseline adoption guide` via README no-new-debt + `docs/baselines.md` + troubleshooting; greenfield path is Quick Start without `baseline create` (strict/advisory).

## CI example verification

- `README.md` GitHub Actions YAML (checkout fetch-depth 0, ruby/setup-ruby, `check --changed --base`) validated via `docs/github-actions.md`; YAML parses, exit codes correct per Labs. Does not fail on shallow history when configured correctly.

## Agent example verification

- Completion snippet inside README (AGENTS.md paste area) based on proven 1.2.0 Commands (`check` + receipt verify must be `fresh`) — Uses authorized trust section, analyzed against `lib/rail_verdict/cli.rb` + `docs/agent-verification.md`.

## Issue / contribution readiness

- `CONTRIBUTING.md` answers where to report + how to validate + how to distribute regression; templates in `.github/ISSUE_TEMPLATE/` (bad, feature, compatibility) that request version/Command/Configuration without secrets.

## Security reporting readiness

- `SECURITY.md` canonical, contact `dev@pedrodalben.com.br`, supported versions explicit, private disclosure demanded (don't publicly disclose exploitable vulns first).

## Adoption tracking readiness

- States (`DISCOVERED` … `BLOCKED`) defined in `docs/adoption/first-10.md`; classification + intensity `INSTALLATION…SECURITY / LAUNCH_BLOCKER…IDEA`; templates prepared; Lab stays oracle. Observability stays voluntary/manual, no telemetry introduced (§39 preserved).

## Launch channel assets

- `docs/launch/show-hn.md`, `reddit-ruby.md`, `reddit-rails.md`, `linkedin.md`, `try-challenge.md` — each channel-adapted, technical, trade-off-aware, asks for Critique. No Spam identical text.

## Branch / Lab protection review

- `master` / Lab `main` protection NONE — intentional solo-maintenance minimal-process model documented (PR discipline without forcing) — `docs/release/1.2-public-surface-audit.md` ACCEPTED, `docs/roadmap/1.3-evidence-backlog.md` EV-08 tracks.

## Discussions decision

- GitHub Discussions not enabled — Issues + adoption templates provide enough surface for launch; re-evaluate after 10 adoption attempts if Q&A volume justifies.

## Known limitations (Launch-Report Annex)

Inline listing per release-closeout: Ruby 4.0 beyond ; Waiver-state vs verification HR; Snapshot non-linearizability; Inspector drift conditional; No brakeman/OS sandbox/full graph/signing ; FOUNDATION_PRIVATE_PATTERNS not run (non-blocking).

## Final public-surface audit

- `docs/release/1.2-public-surface-audit.md`: BLOCKER 0, HIGH 0, MEDIUM 1 (Release notes strengthening, non-blocking), LOW 2 fixed + 1 accepted → PASS.

## Regression

- `docs/release/1.2-regression-report.md`: full package/build/clean install/Lab/launch-demo/receipt fresh→stale/JSON verification → PASS. No runtime regression post-docs.

---

**Final recommendation:** LAUNCH — publish launch posts using `docs/launch/*`; monitor `docs/adoption/first-10.md` manually; treat correctness bugs via patch-policy (EV-01.. hatch 1.3 backlog evidence-driven).

