# RailVerdict 1.2.0 — Lab Campaign

**Date:** 2026-08-24
**Lab commit:** `f2c45ac` (railverdict-lab main)
**Candidate:** `d49c204b36fe89a6fcf5f22fd4c42978362b10d2` (RailVerdict v1.2.0)
**Gem:** `d8e848b1e72585b7f583a85d2a67e25b472bd1860549d1d4330c8a33104f82e9` (published RubyGems, also /tmp/rail_verdict-1.2.0-rubygems.gem)
**Lab runner:** `scripts/lab_run --all` (disposable Rails/Git fixtures, public CLI + MCP only, no internal class loading)
**Infrastructure self-tests:** 28 runs, 1032 assertions, 0 failures (lab `bundle exec rake test`)

## Campaign identity boundary

Lab tests the **exact published gem** (`gem install /tmp/rail_verdict-1.2.0-rubygems.gem` or RubyGems fetch), not a local checkout/path/Git dependency. Documented in `lab/candidate.yml` (`source_sha d49c204`, `gem_sha256 d8e848...`).

## Results (2026-08-24 run on lab main)

| | Count |
|---|---:|
| Total scenarios | 81 |
| **PASS** | **80** |
| **FAIL** | **1 (RVLAB-16)** |
| **BLOCKED** | 0 |
| SKIPPED | 0 |
| Categories | 14 (all green except `repair_anti_cheating` 6/7) |

Detail from `artifacts/lab-run-summary.json` (lab main, see `docs/release/1.2-closeout.md` §10):

- PASS includes: clean PASS, RuboCop new offense → FAIL, RSpec failure → FAIL, Minitest failure → FAIL, required analyzer unavailable → INCOMPLETE, required analyzer timeout → INCOMPLETE, malformed JSON → INCOMPLETE, baseline existing debt PASS, new debt FAIL, waiver accepted/expired, changed-scope PASS/FAIL, invalid Git base → INCOMPLETE, shallow history → INCOMPLETE, JSON/SARIF contracts, PR Intelligence PASS/FAIL, receipt creation/freshness, stale after HEAD/track/index/untracked/config/baseline/waiver mutation, repository mutation during verification fail-closed, MCP execution, repair workflow.

- Single FAIL: `RVLAB-16` *Repair rejects waiver injection* — Lab oracle expects `FAIL` (adding a waiver instead of fixing source should not become PASS), observed `gate: PASS` with `target_status: still_present` + `verification_boundary_changed: {waivers:true, ...}`. This is **inherited from accepted 1.0.1 campaign** (Lab VALIDATION_REPORT cycle 1), not a 1.2 regression — 1.2 actually resolved 3 prior FAILs (`RVLAB-REFUSE-01`, `GIT-01`, `DETERMINISM-01` now PASS). Under 1.2, receipt-side mitigation is proven via `RVLAB-AGENT-12` (receipt `waivers_changed` staleness) and `verify_repair`'s explicit `verification_boundary_changed` signal.

## Scenario families (required by goal)

All families present: clean PASS, offense FAIL, test FAIL (both frameworks), unavailable/timeout/malformed → INCOMPLETE, baseline, waiver (valid/expired/rejected), changed-scope (PASS/FAIL/invalid base), shallow clone, JSON/SARIF contracts, PR Intelligence (PASS/FAIL/INCOMPLETE/invalid base), receipts (create, freshness, each mutation, multi-mutation, malformed/schema/cross-repo/cross-machine/checkout/amend/reset, mutation during verification), MCP, repair/agent workflow.

## New 1.2 surfaces validated

- Repository State Identity v1 (HEAD + index snapshot + worktree delta with per-path hashes + effective inputs) — deterministic `sha256:` over `CanonicalJSON`.
- Verification Receipt v1 (receipt_id, freshness `fresh/stale/invalid/unavailable`, deterministic reason codes).
- Snapshot guard (`repository_changed_during_verification`).
- MCP `get_verification_receipt` / `get_pr_intelligence` single-execution + stale refusal.
- PR Intelligence stable projection digest.
- Dogfood hardening 1.2: native SimpleCov, message sanitization, canonical `unknown` versions, large output bounds — all green.

## Fresh execution note

Historical Lab results not treated as proof; new explicit campaign run against current candidate on 2026-08-24 with corrected SHA is the evidence. Report artifacts are in lab repo; counts derived from `lab-run-summary.json`.
