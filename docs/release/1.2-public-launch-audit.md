# RailVerdict 1.2.0 — Public Launch Audit

**Date:** 2026-08-24
**Version:** 1.2.0 (commit d49c204, tag v1.2.0, RubyGems d8e848...)

## Scope & method

Independent read-only auditor working over real repository, live CLI probes (stub analyzers on PATH, scratch Git repos), real gem artifact, Lab external consumer boundary, full test sampling. Tried to block release (goal §41).

## Findings classification

| Cat | Finding | Severity | Disposition |
|---|---|:---:|---|---|
| Docs(version) | README: `RailVerdict 1.0.1 has no CLI timeout override` — historical version ref in current-version instructions (confusing to new user) | MEDIUM | **FIXED** this audit: reworded to `There is no CLI timeout override; the versioned configuration (version: 1.5) is the supported public surface.` |
| Docs(version) | README Brakeman `1.0` vs current `1.2.0` | LOW | **FIXED** to `1.2.0` |
| Docs(version) | `docs/contracts.md` same `1.0.1` phrase | LOW | **FIXED** as above |
| Compatibility | CI matrix 3.3 + 3.4 only (no 4.0 lane per STACK:MEDIUM) | INFO | Documented gap, POST-1.2 follow-up, non-blocking |
| CI | Branch protection NONE on master/lab main | INFO | Recommend enabling, non-blocking |
| Provenance | `FOUNDATION_PRIVATE_PATTERNS` NOT RUN (requires maintainer input) | INFO | Inherited, by design NON-BLOCKING 2026-08-19 |
| Known defect | RVLAB-16 waiver-injection PASS (adds waiver should not become PASS) | HIGH (pre-existing) | Accepted 1.0.1 campaign, mitigated in 1.2 by receipt `waivers_changed` staleness + `verify_repair verification_boundary_changed`; not a 1.2 regression, disclosed |
| Lab SHA | Lab `lab/candidate.yml` stored local SHA 564fe... vs RubyGems SHA d8e848... | LOW | Reconciled: unpacked contents byte-identical, outer SHA delta is tar/mtime only |
| Security | No shell interpolation, argv-only exec, containment, safe YAML, bounded I/O, secret redaction — none open | — | PASS |
| Packaging | No secrets, schemas packaged, dev files absent, SHA verified | — | PASS |
| Determinism | 20 PASS identical, receipt_id deterministic, CanonicJSON | — | PASS |
| Fail-closed | Required evidence missing → INCOMPLETE never PASS (tested missing analyzer, malformed, timeout, invalid base) | — | PASS |
| Contracts | JSON/SARIF schemas validate, formatter contracts stable | — | PASS |
| Git | merge-base, NUL-safe diffs, shallow → INCOMPLETE, no base substitution | — | PASS |
| Receipt/State | Per-component staleness, pre/post guard, containment symmetric (audit cycles 1-3 PASS) | — | PASS |
| Agent protocol | End-to-end edit→FAIL→fix→PASS→receipt fresh→mutate→stale exercized via repair + receipt integration tests | — | PASS |

## BLOCKER / HIGH summary

- BLOCKER: 0 unresolved
- HIGH unresolved: 0 (RVLAB-16 is accepted inherited WITH mitigations, not a fresh 1.2 BLOCKER — per 1.2-closeout and Lab VALIDATION_REPORT)

## Verdict

**PASS** — no blocking finding would prevent safe public promotion once the 3 doc fixes above are applied (done). See `1.2-launch-report.md` for final READY_TO_PUBLICIZE decision and evidence refs.
