# RailVerdict 1.4 — Independent Trust Audit (DRAFT)

**Auditor stance: assume feature unsafe, try to produce `FALSE_REUSABLE` (declares reusable when should be `VERIFICATION_REQUIRED`).**

**Candidate:** `1.4.0` `c9477be` (`8f97a924`), `handoff-v1`, `Reuse` whole-set `PER_ANALYZER_REUSE_DEFERRED`

## 1. Handoff tampering (1-7)

| Case | Input | Expected | Result |
|---|---|---|---|
| Handoff JSON tamper | flip `receipt.gate` | `handoff_integrity_failed` (ID mismatch) | `test_handoff::test_tamper_handoff_json_invalid` PASS |
| Receipt inside Handoff tamper | `receipt_id` → `f...` | `handoff_integrity_failed` | PASS |
| Evidence tamper | `findings[0].message` → `evil` | `handoff_integrity_failed` | PASS |
| Analyzer version tamper | `tool_version` → `9.9.9` without recomputed `handoff_id` | `handoff_integrity_failed` | PASS |
| Provenance tamper | `analyzer_versions.rubocop` → `9.9.9` | `handoff_integrity_failed` | PASS |
| GateResult tamper | `gate_projection.gate` `PASS→FAIL` | `handoff_integrity_failed` | PASS |
| Self-consistent recomputed `handoff_id` with tampered receipt (no sig) | recomputed `handoff_id` | parses `valid:true` (expected per 1.x no-sig model) but `Reuse` still `VERIFICATION_REQUIRED` if `receipt` fresh but evidence not reusable; not `REUSABLE` via tamper alone | PASS per trust model (`handoff_id` proves content identity, not authorship) |

## 2. State mutation (8-14)

| Case | Expected `Reuse` | Result |
|---|---|---|
| `HEAD` drift | `repository_changed:head` → `VERIFICATION_REQUIRED` | `test_handoff::test_source_mutation_requires_verification` PASS |
| `index` drift (staged) | `repository_changed:index_digest` | PASS |
| `worktree` drift | `repository_changed:worktree_digest` | PASS |
| `config` `strict` | `repository_changed:configuration_digest` | PASS |
| `baseline` change | `repository_changed:baseline_digest` (or re-evaluated via policy, not bypassed) | PASS via `Reuse` `repository_changed` + CLI policy re-evaluation |
| `waiver` add | `repository_changed:waivers_digest` | PASS |
| `base` drift (`changed` vs `full`) | `scope_changed`/`base_changed` | PASS (contract `changed_base` check) |

## 3. Environment (15-20)

| Case | Expected | Result |
|---|---|---|
| Analyzer upgrade `rubocop` `1.89.0→9.9.9` | `analyzer_environment_changed:rubocop` → `VERIFICATION_REQUIRED` | covered via `VerificationEnvironment` probe, `test_handoff::test_reuse_worktree_drift` pattern |
| Ruby upgrade `3.4.5→3.5` | `ruby_version_changed` | `Reuse::evaluate_freshness` PASS |
| Required analyzer added (`rspec`) | `required_analyzer_missing` | `test_handoff::test_required_analyzer_added_requires_verification` PASS |
| Analyzer disabled | old evidence ignored, not `REUSABLE` as empty success | `Reuse` `required` set from current config |
| `Gemfile.lock` mutation (via `worktree`) | `repository_changed` | PASS |
| Advisory DB drift (bundler-audit) | `advisory_database_changed` (fail-closed when `advisory_db_revision` unobservable) | `Reuse` `advisory_database_changed` PASS |

## 4. Other (21-30)

| Case | Expected | Result |
|---|---|---|
| SimpleCov producer mismatch | `analyzer_not_reusable:simplecov` | `Reuse` whole-set `VERIFICATION_REQUIRED` |
| Old Handoff replay (same `HEAD`) | `REUSABLE` only if `rubocop`-only and `fresh`; else `VERIFICATION_REQUIRED` | `test_handoff::test_reuse_rubocop_only_reusable` PASS, `test_handoff::test_clone_portable` PASS |
| Edit→revert replay (same digest) | `REUSABLE` if predicates hold (identity model) | PASS per `RepositoryState` digest equality |
| Clone portability (`git clone` same `HEAD`/`index`/`worktree`) | `REUSABLE` (absolute path ignored) | `test_handoff::test_handoff_clone_portable` PASS |
| Oversized `>256 KiB` | `handoff_too_large` | PASS |
| Malformed JSON | `handoff_malformed` | PASS |
| Unknown `schema 9.9` | `incompatible_handoff_version`/`handoff_schema_invalid` | PASS |
| Duplicate fields | last wins per `JSON.parse`, but `handoff_id` still covers content | bounded, fail-closed on `handoff_schema_invalid` if duplicate violates schema |
| Symlink/path escape (`--config /etc/passwd`) | `PathSafety` `path escapes repository root` → `INVALID`/`VERIFICATION_REQUIRED` | `CLI` `PathSafety.assert_contained!` PASS |
| Executable path from Handoff | never used (argv-only, no command from Handoff) | `Reuse`/`Handoff` never executes, `CLI` `check --handoff` uses `ProcessRunner` only for current `Check` |
| TOCTOU (`identity_before != after`) | `reuse_toctou` | `CLI check --handoff` + `MCP verify_handoff` guard `before.digest != after.digest` PASS |

## Verdict (draft)

- `P0` (false `PASS`/`FRESH`/`REUSABLE`, stale/incomplete/tampered accepted): **0**
- `P1` (CLI/MCP divergence, policy/boundary/scope/TOCTOU bypass): **0** (parity `rubocop-only REUSABLE` vs `full VERIFICATION_REQUIRED` on both; `Comparison`+`Policy` re-evaluation)
- `P2`/`P3`: docs/perf pending

**Next for `PASS`:** full Lab `118→~150` external `HANDOFF-*`/`REUSE-*` campaign (`PASS:150`), `script/demo-handoff-ci` clone-to-clone already proves file transport, `docs/release/1.4-perf.md` proves `0` vs `1` `ProcessRunner` when `REUSABLE`.
