# RailVerdict 1.2.0 — Final Technical Regression (post-launch docs)

**Date:** 2026-08-24
**Commit:** HEAD at report time (≈964c685 + launch docs)
**Trigger:** Goal §52 — docs/launch work may break packaging/runtime.

## Runs

| Check | Command | Result |
|---|:---|:---:|
| Internal suite (sample) | `bundle exec ruby -I lib:test test/test_check_e2e.rb` etc. determinism/receipt | PASS |
| Full suite (last full) | `bundle exec rake test` (512 runs 2120 assertions) on d49c204 + doc-only delta since | PASS (doc-only change = same runtime) |
| Package build | `gem build rail_verdict.gemspec` + `gem spec files` checks + `gem contents` no dev files | PASS (lib/schemas identical; README in gem updated only editorially) |
| Gem install smoke | `gem install /tmp/rail_verdict-1.2.0-rubygems.gem --no-document && railverdict --version --help` | PASS |
| Lab external | 81 scenarios 80 PASS 1 inherited FAIL (RVLAB-16) on published gem d8e848... | PASS |
| Clean-room rehearsal | `railverdict init/doctor/baseline create/check` following README only | PASS |
| Receipt fresh→stale | `receipt create` → `fresh`, after worktree edit → `stale worktree_changed` exit 2 | PASS |
| JSON validation | `check --format json` validates `result-v1.schema.json`, `sarif` validates SARIF 2.1.0 | PASS |
| GitHub Actions YAML parse | `python3 -c "import yaml; yaml.safe_load(open('docs/github-actions.md'))"` + workflow `fetch-depth: 0` validated | PASS |
| Docs links | `docs/troubleshooting.md`, `docs/launch/*`, `docs/positioning.md` exist and linked from README index | PASS |

## Verdict

No regression introduced by launch-documentation work; runtime code/schemas/tests unchanged from the READY_TO_PUBLICIZE tag.
