# RailVerdict 1.6 — Current State (G0 Reality Reconstruction)

Date: 2026-09-05. All values revalidated locally/remotely this session.
Git, GitHub, RubyGems, installed artifacts and reproducible logs win over prior text.

## 1. Canonical SHAs

| Repo | Local HEAD | Remote HEAD | Status |
|---|---|---|---|
| `RailVerdict` (`master`) | `d71671cbbdd0644e3abb6e66b3bcbc8f62ff23ac` | `origin/master` identical | clean, in sync |
| `railverdict-lab` (checked out `lab/1.5-adoption`) | `f555b50ef51ac3b660031b49f844527682672953` | `origin/main` = `9de2ac2417ed4c71f1d22d96eb04d09459d3ada6` | see §2 |

- `RailVerdict::VERSION = "1.6.0"` (`lib/rail_verdict/version.rb`).
- 1.6 commit: `d71671c feat: RailVerdict 1.6 - Rails Change Intelligence & Reviewer Focus`.
- 1.5 commit (parent chain): `ba713d8 feat: RailVerdict 1.5 - Rails-Native Adoption & CI Efficiency`.
- CI of 1.6 commit: PASS on Ruby 3.3 and 3.4 (per prior survey; re-check `gh run list` before release).

## 2. Branches

RailVerdict remote branches: `master`, `feat/1.2-agent-verification-protocol`,
`feat/1.3-verification-freshness`, `fix/1.2-dogfood-hardening`. No 1.5/1.6 feature
branches. Local `master` clean, tracks `origin/master`.

Lab: local `main` = `29b9636` (1.3 identity), **behind `origin/main` (`9de2ac2`, merge of
`lab/1.4-handoff`, 132 scenarios + 1.4.0 canonical identity)**. Checked-out
`lab/1.5-adoption` = `f555b50` with **uncommitted working-tree edits**:
`artifacts/candidate-identity.json`, `lab/candidate.yml`, `lab/scenarios.yml`
(retarget 1.5.0 → 1.6.0 installed-mode + 10 `RVLAB-CI-*` scenarios, 348 insertions).
`lab/ci-01..10` branches exist **locally only** (created by the 1.6 campaign,
unpushed). No other stash entries.

## 3. Tags / Releases / RubyGems

- Remote tags (RailVerdict): `v1.0.0 v1.0.1 v1.1.0 v1.2.0 v1.3.0 v1.4.0`.
  **No `v1.5.0`, no `v1.6.0`.**
- GitHub Releases: latest `v1.4.0` (2026-08-26); then v1.3.0, v1.2.0, v1.1.0, v1.0.0.
- RubyGems `rail_verdict`: latest **`1.4.0`** (2026-08-26, 267 downloads at survey).
- Locally built 1.6.0 gem (this session, `gem build`, single build):
  `rail_verdict-1.6.0.gem`, SHA-256
  `35e88be1a524f27b851916be5ef80850e7b8f177c0301c134ecbc18026d37efe`
  — byte-identical to the SHA recorded in the lab working-tree
  `candidate-identity.json` / `candidate.yml` retarget. Installed isolated to
  `/tmp/rv160-check`, `railverdict 1.6.0`, CWD-independent.

## 4. PRs

- RailVerdict: **no open PRs** (5 merged: #1, #2, #3, #4, #9).
- Lab: **PR #16 OPEN** — `lab: real MCP coverage + handoff fix (132/132, gem 479526c)`,
  head `lab/1.4-handoff` @ `f256406`, base `main`, opened 2026-08-26.
  Decision: **KEEP / INCORPORATE, not obsolete.** Evidence: head is *not* an
  ancestor of `origin/main` (`9de2ac2` merged the older `ab8442c`-era tree);
  PR16 carries 3 unique commits (`3422115` canonical 1.4.0 identity,
  `b802d0f` genuine MCP protocol/parity/findings/freshness/handoff scenarios
  replacing 7 mislabeled `RVLAB-MCP-FRESH-*` fillers that ran CLI instead of MCP,
  `f256406` handoff roundtrip fix). Its commits are already ancestors of local
  `lab/1.5-adoption`. Reconciliation plan (§8) merges this content; merge itself
  awaits maintainer authorization per release discipline.

## 5. Capability matrix

| Capability | Implemented | Tested | Certified | Tagged | Published | Documented |
|---|---|---|---|---|---|---|
| 1.5 Brakeman native adapter | yes (`Analyzers::Brakeman`) | yes (unit + Lab 5.0) | 136 PASS claimed, branch-local | **no** | **no** (gems only local) | CHANGELOG yes; ROADMAP claims tag — false |
| 1.5 targeted tests + fallback | yes (`TestSelection`, `Resolvers::TestCandidates`) | yes | same as above | **no** | **no** | yes |
| 1.5 per-analyzer tiered reuse (`VerificationPlan`) | yes | yes | same | **no** | **no** | yes |
| 1.5 IntegrarPlus 99.4% case | observed once, isolated | n/a (single case) | exploratory, not general | — | — | honestly scoped in report |
| 1.6 19 Rails surfaces + risk + scope + Review Focus | yes (`ChangeSurfaces`, `ChangeIntelligence`, PR-Intel v1.1) | 17 unit + in-repo lab PASS (this session) | **146/146 PASS recertified this session** (verified artifact `35e88be1…`) | **no** | **no** | CHANGELOG yes; ROADMAP **missing 1.6** |
| 1.6 config 1.6 (`review.sensitive_paths`, `review.risk`) | yes, 1.5 configs compatible | yes | same as above | **no** | **no** | yes |
| 1.6 A/B agent experiment | exploratory, n=1 pair | — | exploratory label, not market proof | — | — | honestly scoped |

## 6. Documentation discrepancies (confirmed)

1. `ROADMAP.md` header frozen at 1.3.0/1.4.0-candidate; phases list ends at 1.5
   (claims `Tag v1.5.0`, which does not exist remotely) and **never mentions 1.6**.
2. `ARCHITECTURE.md` is a Phase-0 fossil: everything "proposed, not present"
   (no gem, no MCP) — stale since 1.0. `PROJECT.md` still points at it as
   "proposed structural enforcement".
3. `docs/mcp.md:50` says `tools/list` enumerates **9 tools**; server registers
   **12** (`+ create_handoff, inspect_handoff, verify_handoff` since 1.4).
   Same staleness in `docs/release/1.2-closeout.md:53`, `1.2-launch-report.md:45`.
4. `railverdict-lab/docs/VALIDATION_REPORT.md` frozen at 1.4.0 / catalog 4.0 /
   118 scenarios — predates 1.5 and 1.6 campaigns.
5. Lab `main` (local `29b9636`, remote `9de2ac2`) represents 1.3–1.4, not the
   certified 1.5/1.6 candidate; catalog/README/manifest cohere with each other
   only on the `lab/1.5-adoption` branch working tree, which is unpublished.
6. `RVLAB-PR-01..04` reference `lab/pr-*` branches existing neither locally nor
   on origin — pre-existing catalog gap, unrunnable before 1.6 too (self-reported
   in the 1.6 report §"Lab green" note).

## 7. Lab 1.6 local changes — real location

Working tree of `lab/1.5-adoption` (uncommitted, preserved, not discarded):
`lab/candidate.yml` + `artifacts/candidate-identity.json` retargeted to gem
1.6.0 SHA `35e88be1…` (mode `installed`, `verified: false`,
`resolution_note: installed command explicitly requested`);
`lab/scenarios.yml` +348 lines = 10 `RVLAB-CI-01..10` scenarios (catalog still
declares `5.0`). Setup branches `lab/ci-01..10` are local-only refs. Scenario
count in tree: **146 total** (matches the 146/146 claim surface).

## 8. Reconciliation plan (no destructive action taken)

1. Merge PR16 (`lab/1.4-handoff` → `main`): genuine MCP + handoff fix.
2. Publish `lab/ci-01..10` setup branches; commit the working-tree retarget +
   CI scenarios as `lab/1.6-closeout` on top of updated `main`.
3. Regenerate `VALIDATION_REPORT.md` from a full `--all` run against the exact
   1.6.0 artifact (build-once SHA `35e88be1…`).
4. Close/supersede nothing by hand: PRs merged or closed with justification,
   history preserved.
5. `main` then represents the last canonically certified version (1.6 candidate).

## 9. G0.2 reproduction ledger (this session — FINAL)

- `script/validate-foundation`: PASS (all runnable gates; provenance NOT RUN —
  maintainer-gated, publication remains blocked).
- `git diff --check`: clean.
- `bundle exec rake test`: **627 runs, 2734 assertions, 0 failures, 0 errors, 0 skips**
  (352.6s, seed 13425).
- Build once + isolated install + `--version` + CWD independence: PASS.
- In-repo `lab/change_intelligence.rb`: PASS (6 scenarios incl. 2 adversarial).
- External full catalog `lab_run --all --artifact rail_verdict-1.6.0.gem`
  (146 scenarios, black-box, isolated gem-home, candidate `verified: true`,
  artifact SHA `35e88be1…` == build-once): **146/146 PASS, 0 FAIL/BLOCKED/SKIPPED**.
- Determinism: two `pr --format json --base HEAD~1` runs byte-identical;
  `verify` via MCP == `check --format json` via CLI (gate `INCOMPLETE`,
  completion `incomplete` — fail-closed on unconfigured analyzers, as designed).
- GateResult invariance: intelligence sections (`review_risk`, surfaces, focus)
  project over the decided `gate_result` without mutating it
  (`gate_result.gate: INCOMPLETE` stable across runs).
- CLI/MCP parity: `verify` parity true; `get_pr_intelligence`/`get_verification_receipt`
  take no arguments (current-state observation) — by design, not drift.
- Adversarial: covered by in-repo lab (2 cases) + full-catalog `--all` (all PASS).
- IntegrarPlus full RSpec: **not re-run** (shared-database corruption risk);
  limitation declared per program rules.

## 10. Release truth (1.4 → 1.5 → 1.6)

1.4.0 is the last fully distributed version (tag + Release + RubyGems, mutually
consistent). 1.5.0 and 1.6.0 exist **only as source candidates** (`ba713d8`,
`d71671c`) with local gem builds (`rail_verdict-1.5.0.gem`,
`rail_verdict-1.6.0.gem` present in tree, untracked artifacts). No history will
be rewritten; no retroactive tags. Candidates are reproducible in order from
their SHAs. Full suite (627/0) + full lab (146/146, verified artifact) now complete.
Recommendation: skip the 1.5.0 publication, release 1.6.0 direct — SemVer impact is
additive only (1.5 configs work unchanged, PR-Intel v1.0 frozen), so no 1.5-only
consumer is stranded; CHANGELOG already documents both. Awaiting maintainer publish
authorization (`READY_TO_RELEASE`, not released).
