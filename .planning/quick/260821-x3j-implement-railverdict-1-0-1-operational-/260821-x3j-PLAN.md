---
phase: quick
task: implement-railverdict-1-0-1-operational-
status: complete
---

# RailVerdict 1.0.1 operational compatibility

Continue the existing partial implementation without touching private consumer
material.

## Tasks

1. **Complete per-analyzer timeout compatibility**
   - Verify schema v1.5, strict validation, configuration loading, Check/Doctor
     propagation, and the unchanged 30-second fallback.
   - Add only the smallest missing regression coverage for all required timeout
     and INCOMPLETE semantics.

2. **Harden bundler-audit evidence parsing**
   - Extract exactly one JSON document after informational stdout prefixes using
     JSON-aware boundaries, reject ambiguous trailing content, preserve command
     failure statuses, and keep malformed/missing output fail-closed.
   - Cover pristine, prefixed, findings, malformed, missing, trailing, and
     non-zero execution cases with synthetic fixtures.

3. **Close public release evidence**
   - Update public docs/changelog/version/examples and add generic Lab coverage
     only if an existing public `railverdict-lab` checkout is present.
   - Run the focused and complete test/quality/package checks; do not publish.

## Verification

- `bundle exec rake test`
- repository quality checks and `git diff --check`
- CLI synthetic config check
- `gem build rail_verdict.gemspec`
