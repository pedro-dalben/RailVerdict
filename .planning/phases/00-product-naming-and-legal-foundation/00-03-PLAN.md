---
phase: 00-product-naming-and-legal-foundation
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/analyzers.md
autonomous: true
requirements:
  - FND-05
  - FND-06

must_haves:
  truths:
    - "Maintainers can inspect dated license, installation, output, cost, compatibility, and disposition evidence for all eleven researched analyzers."
    - "No analyzer is bundled, silently installed, or represented as supported from research alone."
    - "Brakeman is excluded from the committed adapter shortlist until a written product/legal decision resolves its current license boundary."
    - "Ruby and Rails ranges are proposals tied to official maintenance state and named synthetic lanes, not compatibility promises."
  artifacts:
    - path: "docs/analyzers.md"
      provides: "Dated analyzer/license registry, proposed shortlist, and Ruby/Rails support lanes"
      contains: "Brakeman"
  key_links:
    - from: "docs/analyzers.md"
      to: "ARCHITECTURE.md"
      via: "external target-project process boundary"
      pattern: "ARCHITECTURE\\.md"
    - from: "docs/analyzers.md"
      to: "ROADMAP.md"
      via: "Phase 1/2 implementation and fixture ownership"
      pattern: "ROADMAP\\.md"
---

<objective>
Create the dated analyzer/license registry and support proposal that constrain later adapter work (FND-05, FND-06).

Purpose: Prevent technical enthusiasm from erasing license, output-contract, failure, cost, and compatibility limits.
Output: `docs/analyzers.md`.
</objective>

<execution_context>
@/home/pedro/.codex/gsd-core/workflows/execute-plan.md
@/home/pedro/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/00-product-naming-and-legal-foundation/00-CONTEXT.md
@.planning/phases/00-product-naming-and-legal-foundation/00-RESEARCH.md
@.planning/research/STACK.md
@.planning/research/FEATURES.md
@.planning/research/SUMMARY.md
@.planning/ROADMAP.md
</context>

<scope_guard stage="before">
Scope boundary: before Task 1, assert that the repository contains none of the Phase 0-forbidden production surfaces. This plan may create only `docs/analyzers.md`; registry research cannot authorize runtime, packaging, fixture, workflow, release/publication automation/action, or AI/MCP implementation paths.
<automated>ruby -e 'paths=IO.popen(["git","ls-files","--cached","--others","--exclude-standard","-z"], &amp;:read).split("\0"); bad=paths.select { |p| p.match?(%r{\A(?:lib|exe|bin)/}) || p.end_with?(".gemspec") || %w[Gemfile Rakefile].include?(p) || p.match?(%r{(?:\A|/)(?:test|spec)?/?fixtures/(?:analyzers?|rails)(?:/|\z)}i) || p.start_with?(".github/workflows/") || p.start_with?(".github/actions/") || p.match?(%r{(?:\A|/)(?:release|publish|publication)(?:[-_.][^/]*)?(?:/|\z)}i) || p.match?(%r{(?:\A|/)(?:ai|mcp)(?:/|\z)}i) }; abort("forbidden Phase 0 paths: #{bad.sort.join(", ")}") unless bad.empty?'</automated>
</scope_guard>

<tasks>

<task type="auto">
  <name>Task 1: Build the dated eleven-analyzer license and contract registry</name>
  <files>docs/analyzers.md</files>
  <read_first>
    @.planning/phases/00-product-naming-and-legal-foundation/00-RESEARCH.md
    @.planning/research/STACK.md
    @.planning/research/FEATURES.md
    @.planning/phases/00-product-naming-and-legal-foundation/00-CONTEXT.md
  </read_first>
  <action>Create `docs/analyzers.md` with evidence date 2026-08-16 and an evidence convention that distinguishes verified registry/project facts, commercial-use screening notes, proposals, and unresolved decisions. Under the exact heading `Analyzer Registry`, create one parseable Markdown table with exactly these columns in order: `Tool`, `Homepage`, `Observed version`, `License`, `Commercial-use consideration`, `Target-project install`, `Bundled status`, `Proposed version policy`, `Native output`, `Execution cost`, `Failure semantics`, `Disposition`, `Owner / blocker`, `Review date`, and `Evidence source`. Include exactly one non-empty row, in the approved order, for Minitest, RSpec Core, RuboCop, rubocop-rails, SimpleCov, Undercover, RubyCritic, Brakeman, Prosopite, bundler-audit, and strong_migrations. Put durable homepage/evidence URLs and the observed version in distinct cells; use the notes around the table for package URL, publication timestamp, schema stability, Rails relevance, overlap, and license-text URL so none of the required cells becomes ambiguous. Every `Bundled status` cell must equal `External`; the registry is not installation authority. Use the refreshed `rubocop-rails` 2.37.0 observation from phase research rather than the older stack snapshot. Preserve Brakeman 8.0.6 under the current `Brakeman Public Use License`, disposition `HOLD`, and a written legal/product owner/blocker. Under an exact `Technical Shortlist` heading, list only RuboCop/rubocop-rails capability metadata, Minitest/RSpec, SimpleCov, and bundler-audit; Brakeman must be absent from that section and from supported/advertised claims.</action>
  <acceptance_criteria>
    - FND-05: all eleven required analyzers have a parseable row with non-empty homepage, observed version, license, commercial-use consideration, install, external status, proposed version policy, native output, execution cost, failure semantics, disposition, owner/blocker, review date, and source.
    - Brakeman's license is not called MIT and its disposition is HOLD.
    - Every tool is external to RailVerdict; no install or bundling action is authorized.
    - Observed versions are time-bounded facts rather than open-ended support promises.
  </acceptance_criteria>
  <verify>
    <automated>ruby -e 'p=File.read("docs/analyzers.md"); sec=p[/^## Analyzer Registry\s*$.*?(?=^## |\z)/m] or abort("missing registry"); lines=sec.lines.grep(/^\|/); headers=lines.fetch(0).split("|",-1)[1...-1].map(&amp;:strip); expected_headers=["Tool","Homepage","Observed version","License","Commercial-use consideration","Target-project install","Bundled status","Proposed version policy","Native output","Execution cost","Failure semantics","Disposition","Owner / blocker","Review date","Evidence source"]; abort("registry headers mismatch") unless headers==expected_headers; rows=lines.drop(2).map { |l| l.split("|",-1)[1...-1].map(&amp;:strip) }; names=["Minitest","RSpec Core","RuboCop","rubocop-rails","SimpleCov","Undercover","RubyCritic","Brakeman","Prosopite","bundler-audit","strong_migrations"]; abort("registry rows mismatch") unless rows.map(&amp;:first)==names; rows.each { |r| abort("blank/incomplete registry row #{r.first}") unless r.length==expected_headers.length &amp;&amp; r.all? { |v| !v.empty? }; abort("#{r.first} must be external") unless r[6]=="External"; abort("#{r.first} homepage/source missing") unless r[1].include?("https://") &amp;&amp; r[14].include?("https://") }; brakeman=rows.assoc("Brakeman"); abort("Brakeman license") unless brakeman[3].match?(/Brakeman Public Use License/i); abort("Brakeman HOLD") unless brakeman[11]=="HOLD"; shortlist=p[/^## Technical Shortlist\s*$.*?(?=^## |\z)/m] or abort("missing shortlist"); abort("Brakeman shortlisted") if shortlist.match?(/Brakeman/i); abort("rubocop-rails version") unless rows.assoc("rubocop-rails")[2].include?("2.37.0")' &amp;&amp; git diff --check -- docs/analyzers.md</automated>
  </verify>
  <done>The public registry provides enough dated evidence to evaluate every researched analyzer without implying installation or support.</done>
</task>

<task type="auto">
  <name>Task 2: Define proposed Ruby, Rails, and adapter support lanes</name>
  <files>docs/analyzers.md</files>
  <read_first>
    @docs/analyzers.md
    @.planning/research/STACK.md
    @.planning/research/SUMMARY.md
    @.planning/ROADMAP.md
    @.planning/REQUIREMENTS.md
  </read_first>
  <action>Add a separate support-proposal section to `docs/analyzers.md`. Tie Ruby `>= 3.3` to the dated official maintenance state; propose core lanes on Ruby 3.3, 3.4, and 4.0; propose Rails context `>= 8.0` with Ruby 3.3/Rails 8.0 and Ruby 4.0/Rails 8.1 synthetic lanes; keep Rails/ActiveSupport out of runtime dependencies; and state the pre-1.0 EOL-drop policy as a project recommendation. Define that support begins only after the named synthetic lanes and lower/current adapter fixtures pass. Record the committed technical shortlist as RuboCop first, then Minitest/RSpec, SimpleCov, and bundler-audit, with rubocop-rails treated as RuboCop capability metadata; keep Undercover, RubyCritic, Prosopite, and strong_migrations deferred for the documented reasons and Brakeman on HOLD. Link each disposition to its owning roadmap phase and state that versions/licenses must be refreshed at implementation and release.</action>
  <acceptance_criteria>
    - FND-06: official maintenance evidence, proposed version floor, and five deliberate synthetic lanes are explicit.
    - The proposal does not claim that any lane or adapter is already supported.
    - Rails is a target context, not a RailVerdict runtime dependency.
    - The shortlist and every deferral/HOLD agree with the roadmap and locked context.
  </acceptance_criteria>
  <verify>
    <automated>ruby -e 'p=File.read("docs/analyzers.md"); ["Ruby 3.3","Ruby 3.4","Ruby 4.0","Rails 8.0","Rails 8.1",">= 3.3",">= 8.0"].each { |s| abort("missing lane #{s}") unless p.include?(s) }; abort("missing proposal qualification") unless p.match?(/proposal|proposed/i) &amp;&amp; p.match?(/until.{0,80}(lane|fixture).{0,40}pass/i); abort("runtime Rails boundary missing") unless p.match?(/no Rails runtime dependency|must not depend on Rails at runtime/i)' &amp;&amp; git diff --check -- docs/analyzers.md</automated>
  </verify>
  <done>The platform and adapter ranges are explicit, testable proposals with no premature compatibility claim.</done>
</task>

</tasks>

<scope_guard stage="after">
After Task 2, rerun the same forbidden-surface assertion; analyzer documentation must remain external evidence rather than installation or adapter code.
<automated>ruby -e 'paths=IO.popen(["git","ls-files","--cached","--others","--exclude-standard","-z"], &amp;:read).split("\0"); bad=paths.select { |p| p.match?(%r{\A(?:lib|exe|bin)/}) || p.end_with?(".gemspec") || %w[Gemfile Rakefile].include?(p) || p.match?(%r{(?:\A|/)(?:test|spec)?/?fixtures/(?:analyzers?|rails)(?:/|\z)}i) || p.start_with?(".github/workflows/") || p.start_with?(".github/actions/") || p.match?(%r{(?:\A|/)(?:release|publish|publication)(?:[-_.][^/]*)?(?:/|\z)}i) || p.match?(%r{(?:\A|/)(?:ai|mcp)(?:/|\z)}i) }; abort("forbidden Phase 0 paths: #{bad.sort.join(", ")}") unless bad.empty?'</automated>
</scope_guard>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External package/license metadata -> project registry | Mutable registry facts and license terms constrain later integration decisions. |
| Registry disposition -> future adapter implementation | A documentary row must not silently authorize install, bundling, or support. |
| Proposed lanes -> public compatibility claims | Research recommendations require synthetic proof before becoming support guarantees. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | OWASP ASVS 5.0.0 L1 mapping / rationale | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------------------------------|-----------------|
| T-00-07 | Spoofing | analyzer versions/licenses | HIGH | mitigate | N/A — documentary third-party metadata; official source provenance and review dates are the applicable controls. | Parse official source URLs, evidence date, observed version, license, and review date per row. |
| T-00-08 | Tampering | Brakeman disposition | HIGH | mitigate | N/A — legal/product disposition record, not an application authorization path. | Require current Public Use License, explicit HOLD, and exclusion from the parsed shortlist pending written review. |
| T-00-09 | Repudiation | support proposal | MEDIUM | mitigate | N/A — documentary support claim; named synthetic proof is the future control. | Name the exact synthetic lanes/fixtures that must pass and retain proposal labels until evidence exists. |
| T-00-SC | Tampering | analyzer package supply chain | LOW | mitigate | N/A — no package is installed; future adapter phases own package legitimacy. | Install nothing, parse `External` for every row, and make later legitimacy/license review an implementation-phase gate. |

HIGH risks block execution until mitigated. No HIGH risk may be accepted.
</threat_model>

<pr_metadata>
  <title>docs(phase-0): add analyzer registry and support proposal</title>
  <acceptance_and_success>FND-05 and FND-06 are satisfied by eleven complete dated registry rows, a testable Ruby/Rails lane proposal, and explicit adapter dispositions.</acceptance_and_success>
  <risks_and_dependencies>Versions, licenses, and maintenance states drift; implementation and release phases must refresh the dated facts. Brakeman remains externally blocked.</risks_and_dependencies>
  <release_and_publication_impact>None of the listed analyzers or platform versions is advertised as supported, bundled, or installed by this documentation-only plan.</release_and_publication_impact>
</pr_metadata>

## Artifacts this phase produces

New paths: `README.md`, `PROJECT.md`, `PHILOSOPHY.md`, `ARCHITECTURE.md`, `LICENSE`, `NOTICE`, `TRADEMARKS.md`, `SECURITY.md`, `ROADMAP.md`, `docs/foundation.md`, `docs/analyzers.md`, `docs/contracts.md`, `schemas/finding-v1.schema.json`, `schemas/configuration-v1.schema.json`, `examples/finding-v1.json`, `examples/configuration-v1.yml`, `script/validate-foundation`, `docs/adr/0001-deterministic-pass-fail.md`, `docs/adr/0002-external-analyzer-execution.md`, `docs/adr/0003-canonical-finding.md`, `docs/adr/0004-versioned-schemas.md`, `docs/adr/0005-fingerprint-baseline.md`, `docs/adr/0006-no-new-debt.md`, `docs/adr/0007-advisory-ai.md`, `docs/adr/0008-remote-ai-explicit-opt-in.md`, `docs/adr/0009-github-as-an-adapter.md`, `docs/adr/0010-cli-and-json-canonical.md`, `docs/adr/0011-mcp-as-an-adapter.md`, `docs/adr/0012-third-party-license-review.md`, `docs/adr/0013-information-firewall.md`, `docs/adr/0014-apache-2-license.md`, and `docs/adr/0015-separate-trademark-policy.md`.

`script/validate-foundation` subcommands: `schemas`, `links`, `identity`, `legal`, `analyzers`, `contracts`, `adrs`, `security`, `roadmap`, `language`, `provenance`, `whitespace`, and `no-production`; no arguments runs all subchecks.

<verification>
- Direct registry checks find all eleven analyzers, all required fields, refreshed rubocop-rails 2.37.0, and Brakeman HOLD.
- Direct support checks find every proposed Ruby/Rails lane and proof qualification.
- `git diff --check -- docs/analyzers.md` passes.
- Plan 00-07 runs the integrated `analyzers` and `links` subchecks.
</verification>

<success_criteria>
- Maintainers have complete dated analyzer/license evidence.
- Proposed platform support is tied to observable future proof.
- No analyzer, especially Brakeman, crosses from research into implementation authority.
</success_criteria>

<output>
Create `.planning/phases/00-product-naming-and-legal-foundation/00-03-SUMMARY.md` when done.
</output>
