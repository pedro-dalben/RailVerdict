---
phase: 00-product-naming-and-legal-foundation
plan: "05"
type: execute
wave: 1
depends_on: []
files_modified:
  - SECURITY.md
autonomous: true
requirements:
  - FND-11
  - FND-12

must_haves:
  truths:
    - "Maintainers can trace assets, adversaries, trust boundaries, false-PASS risks, and HIGH-severity controls to an owning roadmap phase."
    - "No document mistakes executable-plus-argv process handling for an operating-system sandbox."
    - "Contributors know that public examples are synthetic, repository prose is English-only, and private provenance is prohibited beyond ordinary secret scanning."
    - "Publication remains blocked by any unresolved provenance match or missing required release-surface review, without exposing matched values."
  artifacts:
    - path: "SECURITY.md"
      provides: "Combined ASVS L1 threat model, control register, information-firewall policy, and publication evidence procedure"
      contains: "not an operating-system sandbox"
  key_links:
    - from: "SECURITY.md"
      to: "ARCHITECTURE.md"
      via: "trust boundaries and layer ownership"
      pattern: "ARCHITECTURE\\.md"
    - from: "SECURITY.md"
      to: "ROADMAP.md"
      via: "control implementation and release-gate ownership"
      pattern: "ROADMAP\\.md"
---

<objective>
Combine the Phase 0 threat model and public information firewall into one enforceable security contract (FND-11, FND-12).

Purpose: Make false PASS, hostile repositories, subprocesses, AI, fork, supply-chain, publication, and provenance limits explicit before runtime code exists.
Output: `SECURITY.md`.
</objective>

<execution_context>
@/home/pedro/.codex/gsd-core/workflows/execute-plan.md
@/home/pedro/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/00-product-naming-and-legal-foundation/00-CONTEXT.md
@.planning/phases/00-product-naming-and-legal-foundation/00-RESEARCH.md
@.planning/research/PITFALLS.md
@.planning/research/ARCHITECTURE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
</context>

<scope_guard stage="before">
Scope boundary: before Task 1, assert that the repository contains none of the Phase 0-forbidden production surfaces. This plan may create only `SECURITY.md`; security requirements must not create runtime, packaging, fixture, workflow, release/publication automation/action, or AI/MCP implementation paths.
<automated>ruby -e 'paths=IO.popen(["git","ls-files","--cached","--others","--exclude-standard","-z"], &amp;:read).split("\0"); bad=paths.select { |p| p.match?(%r{\A(?:lib|exe|bin)/}) || p.end_with?(".gemspec") || %w[Gemfile Rakefile].include?(p) || p.match?(%r{(?:\A|/)(?:test|spec)?/?fixtures/(?:analyzers?|rails)(?:/|\z)}i) || p.start_with?(".github/workflows/") || p.start_with?(".github/actions/") || p.match?(%r{(?:\A|/)(?:release|publish|publication)(?:[-_.][^/]*)?(?:/|\z)}i) || p.match?(%r{(?:\A|/)(?:ai|mcp)(?:/|\z)}i) }; abort("forbidden Phase 0 paths: #{bad.sort.join(", ")}") unless bad.empty?'</automated>
</scope_guard>

<tasks>

<task type="auto">
  <name>Task 1: Document assets, trust boundaries, threats, and ASVS L1 controls</name>
  <files>SECURITY.md</files>
  <read_first>
    @.planning/research/PITFALLS.md
    @.planning/research/ARCHITECTURE.md
    @.planning/phases/00-product-naming-and-legal-foundation/00-RESEARCH.md
    @.planning/phases/00-product-naming-and-legal-foundation/00-CONTEXT.md
    @.planning/ROADMAP.md
  </read_first>
  <action>Create the first half of `SECURITY.md` as the canonical threat model. Identify protected gate/evidence integrity, baseline/policy/waiver integrity, source/secrets, CI/publisher credentials, and artifact identity; repository, analyzer, host, optional remote-AI, CI/fork, and release trust boundaries; accidental contributors, malicious forks, compromised dependencies/actions/tools/providers/accounts, and resource/cost abusers. Build a STRIDE-oriented risk register covering false PASS from unavailable/stale/malformed/incomplete evidence, hostile repository paths/config/output, shell/process/resource/cleanup risks, baseline poisoning, prompt injection and transmission, privileged fork execution, dependency/action compromise, artifact substitution, and overstated identity/legal evidence. Assign severity, disposition, required Phase 0 control, residual limitation, verification evidence, and owning Phase 1-9 for each; HIGH threats are blocking under the active policy. Map applicable OWASP ASVS 5.0.0 Level 1 categories/controls while stating that ASVS is a control catalog here, not certification. Require executable-plus-argv, bounded concurrent I/O, monotonic timeouts, process-tree cleanup/reaping, minimal environment, explicit incomplete evidence, least-privilege fork/release boundaries, and build-once publication as later-phase obligations. State exactly that argument/process isolation is not an operating-system sandbox and that hostile analyzers/tests still execute with caller authority.</action>
  <acceptance_criteria>
    - FND-11: all named assets, adversaries, trust boundaries, false-PASS/subprocess/AI/fork/supply-chain/artifact threats, controls, residual limits, and owners are present.
    - Every threat has severity and mitigate/accept/transfer disposition; every HIGH item is phase-blocking until its control is evidenced.
    - Applicable ASVS 5.0.0 L1 control categories are version-qualified.
    - Subprocess isolation is explicitly not described as sandboxing.
  </acceptance_criteria>
  <verify>
    <automated>ruby -e 'p=File.read("SECURITY.md"); %w[Assets Adversaries Boundaries STRIDE false\ PASS subprocess baseline AI fork supply-chain artifact ASVS].each { |s| abort("missing #{s}") unless p.downcase.include?(s.downcase) }; abort("missing HIGH blocking rule") unless p.match?(/HIGH.{0,80}block/im); abort("missing sandbox disclaimer") unless p.match?(/not an operating-system sandbox/i); abort("missing dispositions") unless p.match?(/mitigate|accept|transfer/i)' &amp;&amp; git diff --check -- SECURITY.md</automated>
  </verify>
  <done>RailVerdict has a reviewable security model with explicit control ownership and no false containment claim.</done>
</task>

<task type="auto">
  <name>Task 2: Add the synthetic, English-only, non-echoing information firewall</name>
  <files>SECURITY.md</files>
  <read_first>
    @SECURITY.md
    @.planning/phases/00-product-naming-and-legal-foundation/00-CONTEXT.md
    @.planning/research/PITFALLS.md
    @.planning/phases/00-product-naming-and-legal-foundation/00-RESEARCH.md
    @AGENTS.md
  </read_first>
  <action>Add a separate Information Firewall section to `SECURITY.md` rather than creating another public security file. Define allowed provenance as freshly authored, synthetic, generic public-domain examples and prohibited provenance as private code, data, names, identifiers, business rules, architecture, fixtures, metrics, screenshots, logs, prompts, paths, branches, infrastructure, operational details, and derived replicas. Require English-only source, docs, schemas, examples, workflows/templates, commits, release notes, and artifact metadata, with only individually manifest-listed internationalization fixture exceptions once such fixtures exist; no broad directory exemption. Define scan surfaces as tracked tree, all Git refs/history, generated gem, source/release archives, images/media metadata, documentation, release notes, CI caches/artifacts, and installed-artifact manifest, distinguishing Phase 0 current tree/history checks from Phase 9 artifact enforcement. Define a maintainer-controlled private pattern file outside Git, a non-echoing scan interface, and reports limited to date, revision, scanner/version, surfaces, counts/categories, safe paths, artifact digests, exceptions, and reviewer. Missing corpus or absent future artifacts must be reported as not run/not applicable, never as passed. Any unresolved match blocks publication; a real credential is revoked/rotated before cleanup. State that secret scanning and language heuristics are defense in depth, not complete provenance or language proof.</action>
  <acceptance_criteria>
    - FND-12: synthetic-only and English-only rules, prohibited categories, all required scan surfaces, and exception rules are explicit.
    - Private pattern values remain outside the repository and are never emitted by the validator/report.
    - Current versus future scan coverage is truthful, and absent evidence is never labeled passed.
    - Every unresolved match blocks publication but does not erase evidence or invent a clean review.
  </acceptance_criteria>
  <verify>
    <automated>ruby -e 'p=File.read("SECURITY.md"); ["Information Firewall","synthetic","English-only","tracked tree","Git history","generated gem","archives","media metadata","release notes","CI artifacts","outside Git","never print","blocks publication"].each { |s| abort("missing #{s}") unless p.downcase.include?(s.downcase) }; abort("missing secret-scan limitation") unless p.match?(/secret scan.{0,100}(not|isn.t).{0,60}(complete|substitute)/im)' &amp;&amp; git diff --check -- SECURITY.md</automated>
  </verify>
  <done>One security document now governs synthetic provenance, English-only content, non-echoing review, and truthful publication blocking.</done>
</task>

</tasks>

<scope_guard stage="after">
After Task 2, rerun the same forbidden-surface assertion; the security and information-firewall contract must not be accompanied by implementation or release automation.
<automated>ruby -e 'paths=IO.popen(["git","ls-files","--cached","--others","--exclude-standard","-z"], &amp;:read).split("\0"); bad=paths.select { |p| p.match?(%r{\A(?:lib|exe|bin)/}) || p.end_with?(".gemspec") || %w[Gemfile Rakefile].include?(p) || p.match?(%r{(?:\A|/)(?:test|spec)?/?fixtures/(?:analyzers?|rails)(?:/|\z)}i) || p.start_with?(".github/workflows/") || p.start_with?(".github/actions/") || p.match?(%r{(?:\A|/)(?:release|publish|publication)(?:[-_.][^/]*)?(?:/|\z)}i) || p.match?(%r{(?:\A|/)(?:ai|mcp)(?:/|\z)}i) }; abort("forbidden Phase 0 paths: #{bad.sort.join(", ")}") unless bad.empty?'</automated>
</scope_guard>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Planning/research -> public security contract | Recommendations must become precise controls without claiming runtime enforcement exists. |
| Private detection corpus -> public validator/report | Sensitive match terms stay outside Git and matched values never cross into output. |
| Current tree/history -> future release surfaces | Missing gem/archive/media/CI artifacts must remain not applicable rather than falsely green. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | OWASP ASVS 5.0.0 L1 mapping / rationale | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------------------------------|-----------------|
| T-00-14 | Spoofing | security assurance wording | MEDIUM | mitigate | V1 Secure Development Lifecycle (L1 rationale): requirements and verification ownership must be explicit. | Label controls as required/draft and ASVS as a catalog, with future proof owners and residual limitations. |
| T-00-15 | Information Disclosure | private provenance scan | HIGH | mitigate | V8 Data Protection (L1): sensitive matching material and values must not enter logs/reports. | Keep patterns external, report only non-sensitive metadata, and never echo matched values. |
| T-00-16 | Tampering | scan coverage state | HIGH | mitigate | V14 Configuration (L1 rationale): fail closed on incomplete required security evidence. | Distinguish passed, not run, and not applicable surfaces; unresolved matches block publication. |
| T-00-17 | Elevation of Privilege | subprocess/fork assumptions | HIGH | mitigate | V4 Access Control and V14 Configuration (L1 rationale): privileged credentials must not cross untrusted execution boundaries. | State no OS sandbox and require unprivileged secret-free fork execution in its owning phase. |
| T-00-SC | Tampering | supply-chain controls | LOW | accept | N/A — documentary plan installs nothing and creates no workflow/release automation. | Enforce the symmetric no-production scope guards. |

HIGH risks block execution until mitigated. No HIGH risk may be accepted.
</threat_model>

<pr_metadata>
  <title>docs(phase-0): establish threat model and information firewall</title>
  <acceptance_and_success>FND-11 and FND-12 are satisfied by one ASVS L1-aligned document with complete threat/control ownership and a synthetic, English-only, non-echoing provenance policy.</acceptance_and_success>
  <risks_and_dependencies>Automated scans cannot prove English semantics or complete private provenance; manual review and an external private corpus remain required before publication.</risks_and_dependencies>
  <release_and_publication_impact>No release is authorized. Missing private-corpus evidence and any unresolved match continue to block publication, while absent future artifacts are reported honestly.</release_and_publication_impact>
</pr_metadata>

## Artifacts this phase produces

New paths: `README.md`, `PROJECT.md`, `PHILOSOPHY.md`, `ARCHITECTURE.md`, `LICENSE`, `NOTICE`, `TRADEMARKS.md`, `SECURITY.md`, `ROADMAP.md`, `docs/foundation.md`, `docs/analyzers.md`, `docs/contracts.md`, `schemas/finding-v1.schema.json`, `schemas/configuration-v1.schema.json`, `examples/finding-v1.json`, `examples/configuration-v1.yml`, `script/validate-foundation`, `docs/adr/0001-deterministic-pass-fail.md`, `docs/adr/0002-external-analyzer-execution.md`, `docs/adr/0003-canonical-finding.md`, `docs/adr/0004-versioned-schemas.md`, `docs/adr/0005-fingerprint-baseline.md`, `docs/adr/0006-no-new-debt.md`, `docs/adr/0007-advisory-ai.md`, `docs/adr/0008-remote-ai-explicit-opt-in.md`, `docs/adr/0009-github-as-an-adapter.md`, `docs/adr/0010-cli-and-json-canonical.md`, `docs/adr/0011-mcp-as-an-adapter.md`, `docs/adr/0012-third-party-license-review.md`, `docs/adr/0013-information-firewall.md`, `docs/adr/0014-apache-2-license.md`, and `docs/adr/0015-separate-trademark-policy.md`.

`script/validate-foundation` subcommands: `schemas`, `links`, `identity`, `legal`, `analyzers`, `contracts`, `adrs`, `security`, `roadmap`, `language`, `provenance`, `whitespace`, and `no-production`; no arguments runs all subchecks.

<verification>
- Direct checks find every required threat and information-firewall section.
- The security model contains the ASVS level/version boundary and OS-sandbox disclaimer.
- `git diff --check -- SECURITY.md` passes.
- Plan 00-07 validates integrated security, language, provenance, and links behavior.
</verification>

<success_criteria>
- HIGH-severity risks and their future evidence owners are explicit.
- Public material is constrained to synthetic English content.
- Security/provenance reporting cannot leak the private corpus or overstate scan coverage.
</success_criteria>

<output>
Create `.planning/phases/00-product-naming-and-legal-foundation/00-05-SUMMARY.md` when done.
</output>
