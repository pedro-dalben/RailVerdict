---
phase: 00-product-naming-and-legal-foundation
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - PROJECT.md
  - PHILOSOPHY.md
  - ARCHITECTURE.md
autonomous: true
requirements:
  - FND-07
  - FND-13

must_haves:
  truths:
    - "Contributors can identify RailVerdict as a local-first Rails verification framework rather than a SaaS, analyzer, LLM, editor, or autonomous agent."
    - "Contributors can trace objective evidence through deterministic policy to a gate while optional intelligence and consumers remain downstream."
    - "Maintainers can compare RailVerdict with current quality platforms and specialist tools without claiming unsupported superiority."
    - "The proposed repository shape is one gem and one process, while Phase 0 contains no gem or production runtime scaffold."
  artifacts:
    - path: "README.md"
      provides: "Public evidence-before-merge entry point, publication status, non-claims, and complete foundation navigation"
    - path: "PROJECT.md"
      provides: "Public product definition, scope, non-goals, users, constraints, and competitive landscape"
    - path: "PHILOSOPHY.md"
      provides: "Evidence-first, deterministic, local-first, no-new-debt, Rails-first, agent-native, privacy invariants"
    - path: "ARCHITECTURE.md"
      provides: "Four-layer responsibility and dependency contract plus proposed one-gem structure"
      contains: "proposed, not present"
  key_links:
    - from: "README.md"
      to: "PROJECT.md|PHILOSOPHY.md|ARCHITECTURE.md|LICENSE|NOTICE|TRADEMARKS.md|SECURITY.md|ROADMAP.md|docs/foundation.md|docs/analyzers.md|docs/contracts.md|schemas/|examples/|docs/adr/"
      via: "explicit links to every public foundation artifact and the ADR directory"
      pattern: "docs/adr/"
    - from: "PROJECT.md"
      to: "PHILOSOPHY.md"
      via: "product scope links to operating invariants"
      pattern: "PHILOSOPHY\\.md"
    - from: "PHILOSOPHY.md"
      to: "ARCHITECTURE.md"
      via: "invariants link to structural enforcement"
      pattern: "ARCHITECTURE\\.md"
    - from: "ARCHITECTURE.md"
      to: "docs/contracts.md"
      via: "core ownership links to draft public contracts"
      pattern: "docs/contracts\\.md"
---

<objective>
Ratify the public product, philosophy, competitive position, and four-layer one-gem architecture without creating runtime structure (FND-07, FND-13).

Purpose: Give Phase 1 a small, explicit set of boundaries and prevent platform, AI, analyzer, or repository scaffolding from becoming accidental core design.
Output: `README.md`, `PROJECT.md`, `PHILOSOPHY.md`, and `ARCHITECTURE.md`.
</objective>

<execution_context>
@/home/pedro/.codex/gsd-core/workflows/execute-plan.md
@/home/pedro/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/00-product-naming-and-legal-foundation/00-CONTEXT.md
@.planning/phases/00-product-naming-and-legal-foundation/00-RESEARCH.md
@.planning/research/FEATURES.md
@.planning/research/ARCHITECTURE.md
@/home/pedro/.codex/attachments/d21c22a3-b326-4884-b5b4-4a659b07b18d/pasted-text-1.txt
</context>

<scope_guard stage="before">
Scope boundary: before Task 1, assert that the repository contains none of the Phase 0-forbidden production surfaces. This plan may create only `README.md`, `PROJECT.md`, `PHILOSOPHY.md`, and `ARCHITECTURE.md`; it must not create runtime, packaging, fixture, workflow, release/publication automation/action, or AI/MCP implementation paths.
<automated>ruby -e 'paths=IO.popen(["git","ls-files","--cached","--others","--exclude-standard","-z"], &amp;:read).split("\0"); bad=paths.select { |p| p.match?(%r{\A(?:lib|exe|bin)/}) || p.end_with?(".gemspec") || %w[Gemfile Rakefile].include?(p) || p.match?(%r{(?:\A|/)(?:test|spec)?/?fixtures/(?:analyzers?|rails)(?:/|\z)}i) || p.start_with?(".github/workflows/") || p.start_with?(".github/actions/") || p.match?(%r{(?:\A|/)(?:release|publish|publication)(?:[-_.][^/]*)?(?:/|\z)}i) || p.match?(%r{(?:\A|/)(?:ai|mcp)(?:/|\z)}i) }; abort("forbidden Phase 0 paths: #{bad.sort.join(", ")}") unless bad.empty?'</automated>
</scope_guard>

<tasks>

<task type="auto">
  <name>Task 1: Write the product definition, competitive landscape, and philosophy</name>
  <files>README.md, PROJECT.md, PHILOSOPHY.md</files>
  <read_first>
    @.planning/PROJECT.md
    @.planning/phases/00-product-naming-and-legal-foundation/00-CONTEXT.md
    @.planning/research/FEATURES.md
    @.planning/research/SUMMARY.md
    @/home/pedro/.codex/attachments/d21c22a3-b326-4884-b5b4-4a659b07b18d/pasted-text-1.txt
  </read_first>
  <action>Create root `README.md` as the concise public entry point. Lead with the evidence-before-merge position; state that RailVerdict is a documentation-only foundation in Phase 0, publication remains blocked pending launch-jurisdiction and qualified trademark review, it is not a SaaS/hosted service, AI is optional/advisory and unimplemented, and no gem, CLI, analyzer integration, GitHub/MCP adapter, or other product behavior is claimed present. Add explicit relative links to every public foundation file: `PROJECT.md`, `PHILOSOPHY.md`, `ARCHITECTURE.md`, `LICENSE`, `NOTICE`, `TRADEMARKS.md`, `SECURITY.md`, `ROADMAP.md`, `docs/foundation.md`, `docs/analyzers.md`, `docs/contracts.md`, both schemas, both examples, and the `docs/adr/` directory; do not duplicate their content. Create root `PROJECT.md` as the authoritative public definition: RailVerdict is a fully open-source, local-first verification framework for Ruby on Rails applications; it collects evidence from established tools, normalizes findings, applies project policy/baselines, and returns a deterministic gate for humans, CI, and agents. Include the core value, intended users, active scope, explicit non-goals, Phase 0 draft/documentation status, and the publication blocker linked to `docs/foundation.md`. Include a dated, concise competitive landscape covering Qlty/Code Climate, SonarQube, Codacy, Semgrep, GitHub code scanning, MegaLinter, reviewdog, Pronto, Danger, and Rails specialists, separating verified capabilities from the project inference that RailVerdict's position is offline Rails-focused evidence normalization, explicit incompleteness, no-new-debt policy, and stable agent contracts. Avoid marketing claims, implementation claims, or copied stale LineClear conclusions. Create root `PHILOSOPHY.md` separately with the source brief's invariants: evidence before opinion, deterministic gate, no-new-debt adoption, Rails first, local first, fully open source, AI optional/advisory, agent-native machine output, and privacy by design. State the practical boundary for every invariant; cross-link the exact source-brief ADRs for deterministic PASS/FAIL, no-new-debt, advisory/remote AI, CLI/JSON, information firewall, Apache-2.0, and trademark separation, and use `PROJECT.md`, `ARCHITECTURE.md`, or `SECURITY.md` for invariants that do not own a distinct ADR. Do not invent extra ADRs, algorithms, or adapter mechanics.</action>
  <acceptance_criteria>
    - FND-07: product definition, deterministic philosophy, non-goals, and positioning are understandable without planning files.
    - Competitive claims are dated and sourced, and differentiation is labeled as project inference/decision.
    - `PROJECT.md` and `PHILOSOPHY.md` are separate deliverables with distinct ownership.
    - `README.md` states evidence-before-merge positioning, the current publication blocker, and the absence of SaaS, AI, CLI/gem, adapter, and runtime-product implementation claims.
    - `README.md` links every public foundation file, both schema/example pairs, and `docs/adr/`.
    - All selected identity and publication wording matches `docs/foundation.md` by contract, even if that parallel file lands later in Wave 1.
  </acceptance_criteria>
  <verify>
    <automated>ruby -e 'readme=File.read("README.md"); project=File.read("PROJECT.md"); philosophy=File.read("PHILOSOPHY.md"); links=%w[PROJECT.md PHILOSOPHY.md ARCHITECTURE.md LICENSE NOTICE TRADEMARKS.md SECURITY.md ROADMAP.md docs/foundation.md docs/analyzers.md docs/contracts.md schemas/finding-v1.schema.json schemas/configuration-v1.schema.json examples/finding-v1.json examples/configuration-v1.yml docs/adr/]; links.each { |s| abort("README missing link #{s}") unless readme.include?(s) }; abort("README positioning missing") unless readme.match?(/evidence before merge/i); abort("README publication blocker missing") unless readme.match?(/publication.{0,60}block/i); %w[SaaS AI unimplemented].each { |s| abort("README non-claim missing #{s}") unless readme.downcase.include?(s.downcase) }; %w[RailVerdict Rails local-first deterministic Qlty SonarQube Codacy].each { |s| abort("PROJECT missing #{s}") unless project.downcase.include?(s.downcase) }; %w[evidence deterministic no-new-debt local-first advisory privacy].each { |s| abort("PHILOSOPHY missing #{s}") unless philosophy.downcase.include?(s.downcase) }' &amp;&amp; git diff --check -- README.md PROJECT.md PHILOSOPHY.md</automated>
  </verify>
  <done>Public product and philosophy documents define RailVerdict and its evidence-based market boundary without expanding the implementation scope.</done>
</task>

<task type="auto">
  <name>Task 2: Document the four-layer dependency rule and proposed one-gem structure</name>
  <files>ARCHITECTURE.md</files>
  <read_first>
    @PROJECT.md
    @PHILOSOPHY.md
    @.planning/research/ARCHITECTURE.md
    @.planning/phases/00-product-naming-and-legal-foundation/00-RESEARCH.md
    @.planning/phases/00-product-naming-and-legal-foundation/00-CONTEXT.md
  </read_first>
  <action>Create root `ARCHITECTURE.md` with the four one-way layers named evidence collection, deterministic verification core, optional intelligence, and CLI/CI/GitHub/coding-agent/future-MCP consumers. Define that evidence owns facts, policy alone owns gate authority, reporters/platforms project `GateResult`, AI reads immutable results and cannot weaken the gate, and source dependencies point toward core contracts. Include a responsibility table for the future concepts named in the brief without presenting classes as implemented. Add a proposed, not present, Phase 1 one-gem/one-process repository tree containing only paths justified by Phase 1 behavior; explicitly exclude a database, daemon, Rails engine, plugin marketplace, multiple gems, hosted control plane, intelligence directory before Phase 6, GitHub/MCP core types, and abstractions without an owning behavior. State that executable-plus-argv isolation reduces command-injection risk but is not an OS sandbox. Cross-link `PROJECT.md`, `PHILOSOPHY.md`, `docs/contracts.md`, `SECURITY.md`, the relevant ADRs, and `ROADMAP.md` using paths that Plan 00-07 will validate.</action>
  <acceptance_criteria>
    - FND-07: the four layers, responsibilities, gate authority, and dependency direction are explicit.
    - FND-13: one future gem/process is proposed and every directory is tied to an owning phase.
    - The document says the proposed runtime tree is absent during Phase 0.
    - The subprocess boundary is never described as sandboxing.
  </acceptance_criteria>
  <verify>
    <automated>ruby -e 'p=File.read("ARCHITECTURE.md"); %w[evidence deterministic intelligence policy GateResult one-gem one-process].each { |s| abort("missing #{s}") unless p.downcase.include?(s.downcase) }; abort("must label proposed tree absent") unless p.match?(/proposed.{0,40}(not present|absent)/i); abort("must disclaim sandbox") unless p.match?(/not an (operating-system|os) sandbox/i)' &amp;&amp; git diff --check -- ARCHITECTURE.md</automated>
  </verify>
  <done>The repository has an implementation-independent architecture contract with clear one-way authority and no Phase 0 runtime scaffold.</done>
</task>

</tasks>

<scope_guard stage="after">
After Task 2, rerun the same forbidden-surface assertion; README navigation and architecture prose must not be accompanied by runtime scaffolding.
<automated>ruby -e 'paths=IO.popen(["git","ls-files","--cached","--others","--exclude-standard","-z"], &amp;:read).split("\0"); bad=paths.select { |p| p.match?(%r{\A(?:lib|exe|bin)/}) || p.end_with?(".gemspec") || %w[Gemfile Rakefile].include?(p) || p.match?(%r{(?:\A|/)(?:test|spec)?/?fixtures/(?:analyzers?|rails)(?:/|\z)}i) || p.start_with?(".github/workflows/") || p.start_with?(".github/actions/") || p.match?(%r{(?:\A|/)(?:release|publish|publication)(?:[-_.][^/]*)?(?:/|\z)}i) || p.match?(%r{(?:\A|/)(?:ai|mcp)(?:/|\z)}i) }; abort("forbidden Phase 0 paths: #{bad.sort.join(", ")}") unless bad.empty?'</automated>
</scope_guard>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Research/planning text -> public product claims | Historical working-name material and recommendations must not become unsupported public facts. |
| Evidence layer -> policy core | Analyzer facts cross into the only layer permitted to make gate decisions. |
| Optional intelligence/platforms -> deterministic core | AI, GitHub, MCP, and reporters remain untrusted downstream consumers, not authorities. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | OWASP ASVS 5.0.0 L1 mapping / rationale | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------------------------------|-----------------|
| T-00-04 | Spoofing | `PROJECT.md` and `README.md` claims | MEDIUM | mitigate | N/A — documentary product/market claims; dated source attribution is the applicable control. | Date and source observed capabilities; label differentiation as inference/decision and avoid implementation/support claims. |
| T-00-05 | Tampering | `ARCHITECTURE.md` gate authority | HIGH | mitigate | N/A — documentary architecture authority; later implementation maps to access-control/design verification. | State that only policy creates decisions/GateResult and cross-link the exact deterministic-gate ADR plus contract/security docs. |
| T-00-06 | Elevation of Privilege | optional intelligence and platform adapters | HIGH | mitigate | N/A — no adapter code exists in Phase 0; the document constrains future authority. | Keep them downstream/read-only in the architecture and explicitly deny a second gate engine. |
| T-00-SC | Tampering | package-manager supply chain | LOW | accept | N/A — no dependency, gem scaffold, workflow, or package installation is created. | Enforce the symmetric no-production scope guards. |

HIGH risks block execution until mitigated. No HIGH risk may be accepted.
</threat_model>

<pr_metadata>
  <title>docs(phase-0): define RailVerdict product and architecture</title>
  <acceptance_and_success>FND-07 and FND-13 are satisfied by separate product, philosophy, and architecture files with dated competitive positioning and an absent-until-Phase-1 one-gem structure.</acceptance_and_success>
  <risks_and_dependencies>Cross-links target parallel Wave 1 outputs and are validated only after the wave converges; market observations are dated, not permanent claims.</risks_and_dependencies>
  <release_and_publication_impact>Documentation only. It authorizes no gem skeleton, implementation, brand publication, package reservation, or release.</release_and_publication_impact>
</pr_metadata>

## Artifacts this phase produces

New paths: `README.md`, `PROJECT.md`, `PHILOSOPHY.md`, `ARCHITECTURE.md`, `LICENSE`, `NOTICE`, `TRADEMARKS.md`, `SECURITY.md`, `ROADMAP.md`, `docs/foundation.md`, `docs/analyzers.md`, `docs/contracts.md`, `schemas/finding-v1.schema.json`, `schemas/configuration-v1.schema.json`, `examples/finding-v1.json`, `examples/configuration-v1.yml`, `script/validate-foundation`, `docs/adr/0001-deterministic-pass-fail.md`, `docs/adr/0002-external-analyzer-execution.md`, `docs/adr/0003-canonical-finding.md`, `docs/adr/0004-versioned-schemas.md`, `docs/adr/0005-fingerprint-baseline.md`, `docs/adr/0006-no-new-debt.md`, `docs/adr/0007-advisory-ai.md`, `docs/adr/0008-remote-ai-explicit-opt-in.md`, `docs/adr/0009-github-as-an-adapter.md`, `docs/adr/0010-cli-and-json-canonical.md`, `docs/adr/0011-mcp-as-an-adapter.md`, `docs/adr/0012-third-party-license-review.md`, `docs/adr/0013-information-firewall.md`, `docs/adr/0014-apache-2-license.md`, and `docs/adr/0015-separate-trademark-policy.md`.

`script/validate-foundation` subcommands: `schemas`, `links`, `identity`, `legal`, `analyzers`, `contracts`, `adrs`, `security`, `roadmap`, `language`, `provenance`, `whitespace`, and `no-production`; no arguments runs all subchecks.

<verification>
- Direct Ruby content checks pass for all three files.
- `git diff --check -- PROJECT.md PHILOSOPHY.md ARCHITECTURE.md` passes.
- Plan 00-07 validates every relative link and prohibited production path after all Wave 1 outputs exist.
</verification>

<success_criteria>
- Product definition, competitive position, philosophy, and architecture are explicit and mutually compatible.
- Policy remains the sole gate authority.
- The proposed one-gem shape is documented but not created.
</success_criteria>

<output>
Create `.planning/phases/00-product-naming-and-legal-foundation/00-02-SUMMARY.md` when done.
</output>
