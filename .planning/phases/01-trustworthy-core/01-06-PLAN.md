---
phase: 01-trustworthy-core
plan: "06"
title: Check orchestration and end-to-end deterministic verification
type: execute
wave: 3
depends_on: ["05"]
files_modified:
  - lib/rail_verdict/check.rb
  - lib/rail_verdict/cli.rb
  - lib/rail_verdict/doctor.rb
  - lib/rail_verdict/init.rb
  - lib/rail_verdict/findings_command.rb
  - test/test_check_e2e.rb
  - test/test_determinism.rb
  - test/test_command_behaviors.rb
  - test/fixtures/rails_clean/
  - test/fixtures/rails_offense/
  - script/validate-foundation
autonomous: true
requirements:
  - CORE-01
  - CORE-02
  - CORE-08
  - CORE-12
  - CORE-13
  - CORE-14

must_haves:
  truths:
    - "`railverdict check` completes the vertical path offline: configuration, RunContext, safe RuboCop execution, AnalyzerResult, canonical Findings, policy, GateResult, reporters, exit code."
    - "Negative paths (unavailable/unsupported/malformed RuboCop, invalid configuration) exit 2 and can never PASS."
    - "Repeated runs with equivalent inputs produce equivalent canonical content despite locale, timezone, path, ordering, or temp-directory variation."
    - "The synthetic end-to-end fixture passes with real RuboCop evidence in both console and JSON modes."
  artifacts:
    - path: "lib/rail_verdict/check.rb"
      provides: "Deterministic vertical orchestration"
    - path: "test/fixtures/rails_clean/"
      provides: "Synthetic clean Rails-style project (English-only)"
    - path: "test/fixtures/rails_offense/"
      provides: "Synthetic Rails-style project with deterministic RuboCop offenses"
---

<objective>
Wire the vertical slice end to end (CORE-02 offline check, CORE-08 fail-closed proof, CORE-12 modes, CORE-13 exits,
CORE-14 determinism) and close Phase 01: orchestration, command behaviors, synthetic fixtures, real-RuboCop end-to-end
and negative tests, determinism evidence, Phase 00 validator retirement of the no-production gate, and state sync.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Orchestration, init, doctor, findings behaviors</name>
  <files>lib/rail_verdict/check.rb, lib/rail_verdict/cli.rb, lib/rail_verdict/doctor.rb, lib/rail_verdict/init.rb, lib/rail_verdict/findings_command.rb, test/test_command_behaviors.rb</files>
  <read_first>
    @lib/rail_verdict/analyzers/rubocop.rb
    @lib/rail_verdict/verification/policy.rb
    @docs/contracts.md
    @.planning/phases/01-trustworthy-core/01-CONTEXT.md
  </read_first>
  <action>
`RailVerdict::Check.run(config_path:, repository_root:, format:)` returns `[exit_code, stdout_text, stderr_text]`:
load configuration (failure → incomplete result with `configuration` operational failure, rendered per format, exit 2);
probe RuboCop when enabled (version recorded in RunContext analyzer_versions); build RunContext with a revision
resolver that calls `git rev-parse HEAD` through the safe process runner (nil when absent); execute the adapter;
evaluate policy; render console or JSON (JSON mode: diagnostics on stderr only); map exit 0 PASS/WARN, 1 FAIL,
2 INCOMPLETE. An INT trap registers the runner pid registry, and on interruption produces an `interrupted` result and
exit 130 after child cleanup. `RailVerdict::Init` writes the documented default configuration (version 1,
no_new_debt, rubocop enabled+required) to `.railverdict.yml` or `--config PATH`, refuses to overwrite without
`--force`, writes nothing else, and exits 0/2. `RailVerdict::Doctor` is a read-only observation report (doctor 1.0
JSON shape or console) of ruby/rails detection, configuration validity, and RuboCop availability/version/support;
invalid configuration exits 2. `RailVerdict::FindingsCommand` runs the same evidence path and prints the findings
projection (`{"schema_version":"1.0","findings":[...]}` in JSON mode); incomplete required evidence exits 2.
`init` is written through the configuration schema example semantics; `baseline create` stays the plan 01-01 deferral.
Tests prove: init create/refuse/force; doctor observations and exit 2 on invalid config; findings JSON documents
validate against finding-v1; check `--changed`/`--base` remain the Phase 4 deferral.
  </action>
  <acceptance_criteria>
    - CORE-01: all five command behaviors exist exactly as contracted; no extra commands.
    - CORE-02: check runs with no network, account, AI, telemetry, or GitHub dependency.
    - CORE-13: the single result-to-exit mapping produces 0/1/2/130 only.
  </acceptance_criteria>
  <verify>bundle exec rake test && git diff --check</verify>
  <done>All contracted command behaviors work offline.</done>
</task>

<task type="auto">
  <name>Task 2: Synthetic end-to-end, negative, and determinism proof</name>
  <files>test/fixtures/rails_clean/, test/fixtures/rails_offense/, test/test_check_e2e.rb, test/test_determinism.rb, script/validate-foundation</files>
  <read_first>
    @lib/rail_verdict/check.rb
    @SECURITY.md
    @.planning/phases/01-trustworthy-core/01-CONTEXT.md
  </read_first>
  <action>
Create synthetic English-only fixtures: `test/fixtures/rails_clean/` (a `.railverdict.yml`, `.rubocop.yml` pinning a
minimal stable ruleset with NewCops disabled, `Gemfile.lock` advertising rails 8.x and ruby 3.4.x, and clean
`app/models/user.rb` style files) and `test/fixtures/rails_offense/` (same shape plus deterministic offenses in two
or more files, including file order that differs from canonical order). End-to-end tests run the real check against
copied fixture directories in a temp root with the real `rubocop` executable (injected bare-resolver, PATH-based):
clean fixture → exit 0, gate PASS, valid result-v1 JSON; offense fixture → exit 1, gate FAIL, findings sorted
canonically; JSON mode stdout is exactly one schema-valid document with one trailing newline and stderr carries no
gate prose. Negative end-to-end tests (resolver-injected stubs and environment manipulation, never network): missing
rubocop → unavailable → exit 2; unsupported version → exit 2; malformed output → exit 2; invalid configuration file →
exit 2 with `configuration` operational failure in JSON; each asserts the result JSON validates against result-v1 and
shows `gate INCOMPLETE`, `policy_status not_evaluated`, at least one operational failure — proving CORE-08.
Determinism tests (CORE-14): run the same fixture check repeatedly under varied `LC_ALL`, `TZ`, `TMPDIR`, shuffled
resolver observation order, and distinct temp roots; normalized gate content must be byte-identical (JSON document
compared directly; paths in invocation argv excluded only if machine-relative by construction). Update
`script/validate-foundation`: the `no-production` subcheck prints a retirement note and passes once
`lib/rail_verdict.rb` exists (Phase 1 boundary), keeping every other subcheck enforced; run the full validator and
record results. Finally sync `.planning` state (STATE.md, ROADMAP.md plan counts, REQUIREMENTS.md CORE statuses),
write `01-VALIDATION.md` with the evidence table, and produce this summary.
  </action>
  <acceptance_criteria>
    - Synthetic Rails fixture completes the RuboCop vertical path in console and JSON modes with real evidence.
    - Negative fixtures prove incomplete required evidence cannot PASS and exit 2.
    - Determinism variations produce equivalent canonical gate content offline.
    - Full test suite plus foundation validator pass with a clean working tree.
  </acceptance_criteria>
  <verify>bundle exec rake test && script/validate-foundation whitespace && script/validate-foundation language links schemas contracts && git diff --check</verify>
  <done>Phase 01 exit test demonstrated and recorded.</done>
</task>

</tasks>

<verification>
- Real RuboCop end-to-end: clean fixture exit 0 / PASS; offense fixture exit 1 / FAIL.
- Every negative fixture exits 2 with schema-valid INCOMPLETE JSON.
- Determinism matrix (locale/timezone/tempdir/ordering) byte-identical canonical JSON.
- `script/validate-foundation` passes with the retired no-production gate documented.
</verification>

<output>
Create `.planning/phases/01-trustworthy-core/01-06-SUMMARY.md` and `.planning/phases/01-trustworthy-core/01-VALIDATION.md` when done.
</output>
