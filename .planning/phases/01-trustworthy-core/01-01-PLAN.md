---
phase: 01-trustworthy-core
plan: "01"
title: Gem and CLI foundation
type: execute
wave: 1
depends_on: []
files_modified:
  - rail_verdict.gemspec
  - Gemfile
  - Gemfile.lock
  - Rakefile
  - .gitignore
  - exe/railverdict
  - lib/rail_verdict.rb
  - lib/rail_verdict/version.rb
  - lib/rail_verdict/errors.rb
  - lib/rail_verdict/cli.rb
  - test/test_helper.rb
  - test/test_cli_surface.rb
autonomous: true
requirements:
  - CORE-01
  - CORE-13

must_haves:
  truths:
    - "One gem `rail_verdict` exposes exactly one executable `railverdict` under namespace `RailVerdict`."
    - "The CLI parses only the five contracted command surfaces plus global `--help`/`--version` and maps usage errors to exit 2."
    - "`baseline create` parses its draft options and exits 2 with an explicit Phase 3 deferral; `check --changed`/`--base` exit 2 with explicit Phase 4 ownership."
    - "The test harness runs with `rake test` using Minitest only."
  artifacts:
    - path: "rail_verdict.gemspec"
      provides: "Single-gem package metadata with `json_schemer` as the only runtime dependency"
    - path: "exe/railverdict"
      provides: "Single CLI process entrypoint"
    - path: "lib/rail_verdict/cli.rb"
      provides: "Command parsing and the single result-to-exit mapping point"
---

<objective>
Create the one-gem foundation (CORE-01 exit surface skeleton, CORE-13 exit mapping point): gemspec, Gemfile, Rakefile,
executable, namespace, error hierarchy, and a CLI that accepts exactly the contracted five command surfaces with
draft options, rejects deferred boundaries with exit 2, and maps unknown usage to exit 2.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Create gem foundation</name>
  <files>rail_verdict.gemspec, Gemfile, Gemfile.lock, Rakefile, .gitignore, exe/railverdict, lib/rail_verdict.rb, lib/rail_verdict/version.rb, lib/rail_verdict/errors.rb</files>
  <action>
Create `rail_verdict.gemspec` (name `rail_verdict`, version from `RailVerdict::VERSION` 0.1.0, license Apache-2.0,
`required_ruby_version >= 3.3`, bindir `exe`, executable `railverdict`, files limited to lib, exe, schemas, README,
LICENSE, NOTICE; runtime dependency `json_schemer >= 2.5, < 3`; metadata `rubygems_mfa_required true`).
`Gemfile` sources rubygems.org and loads the gemspec. Rakefile defines `rake test` via `Rake::TestTask`
(pattern `test/**/test_*.rb`) as default. `.gitignore` ignores `tmp/`, `pkg/`, `*.gem`, `.bundle/`, `coverage/`.
`exe/railverdict` is a `#!/usr/bin/env ruby` executable that requires lib and exits with `RailVerdict::CLI.new.run(ARGV)`.
`lib/rail_verdict.rb` is the public load boundary requiring version and cli. `errors.rb` defines `RailVerdict::Error`,
`UsageError`, and `ConfigurationError` (with `source_path` and `property_path`). Bundle install commits `Gemfile.lock`.
  </action>
  <acceptance_criteria>
    - `bundle exec rake -T` lists the test task; `bundle exec ruby -e 'require "rail_verdict"'` resolves from lib.
    - Gemspec declares exactly one runtime dependency (`json_schemer`) and no Rails dependency.
    - No additional executables, gems, or package directories are introduced.
  </acceptance_criteria>
  <verify>bundle install && bundle exec ruby -Ilib -e 'require "rail_verdict"; puts RailVerdict::VERSION' && test -x exe/railverdict && git diff --check</verify>
  <done>Gem foundation loads and exposes the version constant.</done>
</task>

<task type="auto">
  <name>Task 2: Implement CLI surface and exit skeleton</name>
  <files>lib/rail_verdict/cli.rb, exe/railverdict, test/test_helper.rb, test/test_cli_surface.rb</files>
  <action>
Implement `RailVerdict::CLI` using stdlib `OptionParser` only. Subcommands: `init` (`--config PATH`, `--force`),
`doctor` (`--config PATH`, `--format console|json`), `check` (`--config PATH`, `--format console|json`, `--changed`,
`--base REV`), `baseline create` (`--config PATH`, `--output PATH`, `--format console|json`), `findings`
(`--config PATH`, `--format console|json`), plus global `--help` and `--version`. `check`/`findings`/`doctor` stubs
return exit 2 with a `not yet implemented in this build` diagnostic until plan 01-06 wires them; no placeholder
behavior may claim a gate. `baseline create` always exits 2 with the Phase 3 deferral message on stderr and writes
nothing. `check --changed` or `--base` exits 2 with the Phase 4 ownership message on stderr. Unknown commands and
unknown options exit 2 with usage on stderr. `--version` prints `railverdict <version>` and exits 0. `--help` prints
the five-command usage and exits 0. Add Minitest `test_helper.rb` (loads lib from repo, `Minitest.autorun`) and
`test_cli_surface.rb` proving every contracted surface parses, deferred boundaries exit 2 with the exact messages,
unknown input exits 2, and `--version`/`--help` exit 0. In JSON format requests, nothing may be written to stdout by
the stubs.
  </action>
  <acceptance_criteria>
    - CORE-01 surface: five commands with exact draft options parse; no extra commands exist.
    - CORE-13 skeleton: a single method maps result state to exit codes 0/1/2/130; stubs and usage errors use exit 2.
    - `baseline create` is a pure deferral boundary that writes no file in any test.
  </acceptance_criteria>
  <verify>bundle exec rake test && exe/railverdict --version && git diff --check</verify>
  <done>CLI surface exists with deterministic usage exits and explicit deferral boundaries.</done>
</task>

</tasks>

<verification>
- `bundle exec rake test` green with the CLI surface tests.
- `exe/railverdict --help` lists exactly the five contracted commands.
- `exe/railverdict baseline create; echo $?` prints 2 and the deferral message.
</verification>

<output>
Create `.planning/phases/01-trustworthy-core/01-01-SUMMARY.md` when done.
</output>
